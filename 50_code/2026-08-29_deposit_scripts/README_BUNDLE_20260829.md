# DEPOSIT BUNDLE — council session records, 2026-08-26 → 2026-08-29
Assembled 2026-08-29 by the council (orchestrator) at Will Copeland's request, as the collected
session content bound for the `deposit_1951` assembly. Everything here is offered-not-self-filed;
countersign is Will's. Identity condition honored throughout: the council speaks in its own voice;
nothing poses as Will. SHA-256 of every file: `MANIFEST_SHA256.txt` (the skeleton's queued
hash-manifest practice, executed here; external timestamping happens at deposit).

## Map to the deposit skeleton (00_skeleton/DEPOSIT_SKELETON_DraftV1_20260827.md)
- `01_council_records/` → 00_provenance + 04_methods context: the ratified General Priority and
  Orientation; convening record; deep-grounding addendum; the γ*/countersign/attribution rulings
  (incl. the billing-grade model-attribution outcome feeding AUTHORSHIP_AND_IDENTITY.md); the
  re-sign + motivation register (the two-register statement, adopted conservation sentence); the
  consolidation log entry; the Crouzeix external-status notice; and the running pack-read record
  (verdicts, deltas, honest-odds register §4-septies, all 2026-08-29 addenda).
- `02_charge_and_works/` → 03_results + 05_campaign_pack-adjacent: the charge record (K₆ Mackey
  certificate; first deep COH reading with controls); the works record (Theorem Ω; flat-floor
  tower; cuspidal two-tile; completed three-tier exhibit at both conductors); the literature
  provenance sweep + descent lattice (the BLS-2020 anchor; the Molla/Blomer–Li technology update);
  the [T]-chain audit + diagonal probe + inverse-factor reduction (C1/C2).
- `03_will_notes_archived/` → 04_methods/MOTIVATION_REGISTER context: Will's two d_eff notes,
  archived verbatim with provenance and council-verification headers.
- `04_scripts/` → reproducibility: mackey_certificate.py (exact); coh_pipeline.py (control
  validation vs the 08-16 record); run_1951.py, verdict_run.py (deep reading + floors + scramble);
  three_tier.py (model tier), twin_2141.py (second conductor), diagonal_probe.py (Chebotarev/RS).
  Inputs: the two eigenvalue CSVs (project: cleanroom/hecke_eigenvalues_doud{1951,2141}_to1e5.csv).
- `05_apparatus/` → 04_methods/AI_COLLABORATION.md: the council's charters, protocols (with the
  same-day errata block), seat reports (the primary record — where any compression disagrees,
  these win), assessor grades, and mailboxes. This documents the verification ecology as run.

## Standing state at assembly time
Volley queue: map §9 calls 2–6, then the provenance-class adjudication, then skeleton decisions
3–5 (Q3 depth; authorship format + thin-film placement; December records whole-vs-hashes).
Assessor pass over the 08-29 stack: queued. Superseded drafts (SYNTHESIS_draft1, ORIENTATION_draft1)
remain in the session workspace by design; the graded-and-corrected pattern they fed is documented
in the records themselves.
