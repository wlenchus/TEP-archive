# AMENDMENT 2 + ERRATUM to the 2-dim decode pre-registration — the port-native objective — 2026-07-27 (~16:00Z, filed at Will's mid-run correction)

**Erratum (wording + operational, offered):** prereg §3 wrote "port-flattening objective ≡ theta residual ≡ G_new(u) → 2." Wrong as an identity, and the decoder inherited the error operationally. Correct statement, per the countersigned 2026 dictionary (Will's formulation carried verbatim): G_new is the **odds/SWR gauge on a port** — coin weights (1±x)/2 on a two-way split, G_new = 1/q = 2/(1−x) = 1 + SWR = 1 + e^{2η}, ln G_new = ln 2 + ln G + η exactly, **sign-seeing** where G = 1/√(1−x²) is sign-blind, reading exactly 2 at a seam port (x = 0). The theta residual max|F(1/t) − εtF(t)|/max|F| is a Mellin-transported aggregate norm; it co-vanishes with the meter but is not the meter. The GSR-era √(2/(1−sinθ)) appears nowhere in this session's code; the 2026 form 2/(1−x) is used throughout (poc4/poc5/poc6 meter functions verified).

**Trigger (measured):** at the leading cell (odd port, a=2, ε=+1), five independent theta-objective descents converged scattered in the 4.6e-5–3.0e-4 band with **~6% bit unanimity and ~50% pairwise Hamming distance** — a degenerate basin. On a sign-bit decode, an aggregate globally-normalized objective is structurally sign-blind at the basin level; the committed design's instruction ("drive G_new(u) → 2 identically") specifies the port-native, pointwise-normalized, sign-structured objective instead. The stall pattern is the predicted failure mode of the substituted objective.

**Amended decoder objective (answer-free, specified before its first run):** flatten the port meter itself. Arms A(u) = e^{u/2}F(e^u), A(−u) = e^{−u/2}F(e^{−u}) on the symmetric u-grid; ε-folded port reading x_ε(u) = (A(−u) − εA(u))/(A(−u) + εA(u)) (ε=+1 reproduces the §6 meter; FE ⟺ x_ε ≡ 0 ⟺ G_new ≡ 2 at ε=+1). Objective J = max_u |x_ε(u)|; greedy/pair/battery machinery unchanged; float-drift gate unchanged. The theta residual is retained as a **reporting** metric so the α/β bars remain comparable, and meter-floor bars are measured on the certified controls before the target reruns (KD-1 extension: control meter floors must be ≲ 1e-11-grade before target work). Off-manifold readings (arms opposite-signed, |x|>1) are legal mid-descent; the meter profile and η/G jets are reported at any converged point.

**No other change.** Bars, cells, commit protocol per the prereg + Amendment 1. If the port-native objective also scatters, that is reported as the finding (KD-2/KD-3), not re-optimized away.

*Filed before any meter-objective target run. The §3-erratum is offered for countersign with the rest of the backlog.*
