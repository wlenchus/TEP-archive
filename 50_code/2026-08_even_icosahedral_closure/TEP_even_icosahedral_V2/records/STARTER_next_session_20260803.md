# Starter prompt — session following 2026-08-03
*(Written by the session-C instance at close. Supersedes `STARTER_next_session.md` (07-27A's, kept for history). Everything persists in the claude.ai project "2026-07-26" under `cleanroom/`; the 08-03 container may or may not survive — all of session C regenerates from `SCRIPTS_BUNDLE` + the gp2 scripts hashed in manifest v6, and every table is deterministic GP output.)*

Hi. You inherit the corpus the morning after the summit was **read**. State, one paragraph: the even-icosahedral 2-dim lift at Doud-1951 has committed Hecke data — 67 sign-bits (58 visible), N = 1951 blind-selected, **parity ρ̃(c) = −I (sin-type Maass partner — measured, contradicting the ledger prediction)**, orientation B (the inherited orientation was wrong — live-cell control won), ε = +0.885096−0.465408i, cert 8.957e-15, conjugate cell independently at 2.649e-15, meter G_new = 2 to 2.2e-14. Commit preceded all graders; the seal did its work and is now LIFTED — Crespo/K̃/lift literature is legal for everyone. Read in order: `decode2dim_COMMIT_20260803.md`, `decode2dim_RESULTS_20260803.md`, `asides_consolidation_log_entry_20260803C.md`, then B's `decode2dim_STATUS_KD2` + `portseam_reconsidered` for the inherited layer.

**0. The decision to make FIRST:** the bit-level external grade — **construct the lift openly (Crespo/K̃ genre, or any modern route) for Doud-1951 and compare traces against the committed table.** This is now an ordinary open computation (no seal to manage) and it is the single decisive step: it either crowns the decode or refutes it, and Will's pre-authorized caveat means the record takes either verdict at full volume. If you and Will prefer consolidation first (countersign ledger — three sessions deep, see `countersign_ledger_20260803.md` — rigorization, referee package), that is legitimate; say so in your opening record.

**1. Queue after that:** (a) mid-band completion + u = a₁₉₅₁ (two routes: finish the twist-arm mid-bands; or derive ε(u) via Tate local constants and solve u from the measured ε — possibly one afternoon, analytically clean). (b) The derived det-1 battery (b⁰_p = b_p(−1)^{j(p)}, N = 1951², a = 0) — closes B's dyadic question by construction. (c) GATE-B + the wall map (big planted rung D = 3,806,549 regenerates from `planted_gen.gp`). (d) Rigorization via Palojärvi–Zhao (2508.03023). (e) The five remaining Doud lifts (2141 first — p ≡ 1 mod 4 admits order-10-nebentypus cells too; pre-register the richer grid). (f) The referee package/deposit — ten sessions of debt now carrying a first-ever dataset and a falsifiable parity prediction.

**2. Conventions (all standing):** tier tags; offered-not-self-filed; pre-register before computing; kill conditions first; commit-with-SHA-256 before graders; parity qualifiers in every title; content-keyed references; training-prior disclosure; inspect-before-cite; honest-bite-first. **New standing rule (session C): every inherited discrete gauge gets a live decode cell, never a spot-check negative** — two inherited conventions (port parity, golden orientation) were both wrong at lift level while perfectly consistent at shadow level; the live cells caught both.

**3. Drift-watch chain, seven deep:** enthusiasm-bias; vocabulary-level agreement; decidability gradient; hand-computation confidence (two more machine-caught instances in C: a meter exponential sign, a ζ₁₀-branch); lexical summit-drift; optimizer-manufactured signal; **inherited-gauge complacency (new, named in C)**. The controls that held: planted rungs before targets, per-arm true-bits lint, whitening by what the solver cannot yet know, and printing self-contradicting measurements at headline volume.

**4. Session-ops delta (beyond A/B's):** exact Bessel kernels — no splines (4K₀(2y), 4yK₀(2y); scipy k0 + mpmath besselk/meijerg triple validation, 3.7e-16); the complex-nebentypus control (L(η)², η quintic mod 11 — Gauss-sum ε matched to 2.3e-16) is mandatory before any complex-coefficient target; whitened multi-arm objective = defect/σ² with σ² = Σ_undecided candidate-gap variance (clamp σ² ≥ 0 against fp cancellation); μ₁₀-marginal ramified ports contribute σ² too (R2); solver = staged bands × beam-128 (S2) with exhaustive-prefix S5 (beam-16 tails) as the second family; single-path solvers fail — do not spend context rediscovering this (A20).

**5. Working with Will:** unchanged from A's starter, and the record strengthened it again: the retarget that made the summit tractable came from taking the field's own signage (prime conductor) at face value against an inherited framing, which is exactly the move his corrections model. The backlog is three sessions deep — offer the ledger early.

The corner reads, and now it is also *written*: sixty-seven signs of it, committed and conjugate-replicated, with the constructor's grade waiting. Say which mountain you're on in the first line — and if the grade comes back against the bits, that goes in the first line too.

Good hunting.
