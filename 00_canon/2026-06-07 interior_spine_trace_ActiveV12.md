# TEP / Entrodynamics — Interior spine-trace (verification companion)

**What this is.** A companion to `6-7_early_corpus_synthesis.md`. That document is a lossless archive of the *foundations*; the June endpoints (`6-1` / `6-2` / `6-7`) have been read and are held as claims. This document covers the **interior** — the files between the early set and the June endpoints (≈ late-Feb → May) — with a different instrument. Not a lossless re-synthesis (the interior is long and partly intermediate/superseded), but a **spine-trace**: for each result that actually reaches the June endpoints, find where in the interior it is *earned*, independently verify that load-bearing step, and label its role. Conclusions of the source documents are recorded as claims; my own verification status is attached independently and does **not** inherit their self-grading.

**Role labels.**
- **[load-bearing]** — carries forward to the June endpoints; part of the spine.
- **[scope-boundary]** — an honest negative or limitation that *bounds* a claim. These matter as much as the positives.
- **[recognition]** — correctly identifies a standard piece of mathematics. Real, but its evidential weight is recognition-of-structure, **not** forcing/derivation.
- **[branch / superseded]** — a sideline that doesn't reach the endpoints, or a retired result.
- **[dropped / demoted]** — a substantive insight the corpus raised and then set aside (parked as speculative, or simply not carried forward). The endpoint-anchored trace is structurally *blind* to these, so they are tracked deliberately.
- **[provenance correction]** — where a later document already resolved, refuted, or relocated a claim that an earlier document frames confidently, so that reading the earlier doc alone over-rates it. (Genuine instance, below: `4-30`/v0.5 frame the Hodge sector-identification confidently; `5-29` had already refuted its strong form. *Not* an instance: the `2-16`/Doppler attribution from an earlier session — that document simply had not been uploaded yet, so there was nothing to alias; once it was, it folded in.)

**Verification tags.** `[VR]` = reproduced symbolically/numerically by me this session (sympy 1.14 / numpy 2.4). `[VR-doc]` = the document's own machine-precision verification, structure confirmed by me but not independently re-run at scale. `[claim]` = interpretive/physical, recorded not endorsed.

Order is chronological-but-selective; depth is allocated by load. **Input safeguard (adopted after cluster `4-30`):** the spine-trace is the *output* lens, but the *input* is a full read of each interior file — not a topic-matched search — precisely because a topic-matched retrospective aliases out (a) insights mentioned only in passing in off-topic sections and (b) valuable lines the framework dropped. Each cluster therefore carries an explicit **negative-space note** recording what was read in full vs skimmed, plus running `[dropped/demoted]` and `[provenance correction]` registers, so the residual aliasing risk is *auditable* rather than invisible.

---

## Cluster `4-30` — N-oscillator chains: how the 2-DHO budget generalizes, and where it stops

**Files.** `4-30_dho_chain_finding_v2`, `4-30_schur_feshbach_chain_note`, `4-30_iterative_reduction_and_primes` (+ `5-10_iterative_reduction_addendum`, a short literature-placement coda).

**What the cluster does.** It is the direct successor to the early DHO base (early-synthesis Part VI, `2-16`/`2-17`): does the 2-DHO Lorentz/Doppler/budget structure survive composition into N-oscillator chains and general graphs? The answer is a mix of one durable exact identity, one bounded generalization whose clean failure is *itself governed by the number theory of the chain length*, a verified boundary-operator (Schur-holography) reduction, and one standard-mathematics recognition (the metallic-mean spectrum). It is also where I first over-elevated the Hodge sector-identification (corrected below).

> **Negative-space note (fine-grit pass).** The foundational pass over this cluster traced the proven results at a generous resolution; a finer-grit read of the "implications," "open-directions," and Schur-Feshbach connection sections then recovered four further items — the `σ₂(ζ)` prefactor, the prime-structure *role* (first mis-tagged as a non-load-bearing branch), the sinc² nonlinear budget, and the Schur-holography boundary operator. This is the intended two-resolution workflow — a solid foundational pass, then a fine-grit supplement that surfaces overlooked nuggets *and* worked resolutions to previously-open threads — not a failure of the first pass. The registers and input safeguard exist because that finer pass reliably yields such items. **One caution it does *not* cover:** the safeguard guards against *within-file* omission; it did **not** prevent a *cross-cluster* over-elevation — I bundled the (true) Schur-holography reduction with the (refuted) Hodge sector-identification, which `5-29` had already taken apart. Read in full: all four files.

### The load-bearing through-line

- **Universal product law [load-bearing] [VR].** For any symmetric-coupling graph (path / cycle / star / complete / tree / arbitrary), uniform `ω_n`, any damping: `Π_j ω_j = ω_n^N · √det(I − K)`, with `ω_j` the lossless normal-mode frequencies and `K` the coupling matrix (`K_ii = 0`). Verified exact to `10⁻¹⁶` across path-4, cycle-4, star-4, complete-4, Y-tree, random-5/6. **My read:** this is exact and topology-universal precisely *because* it is the determinant–eigenvalue identity in budget dress — `ω_j² = ω_n²(1 − λ_j(K))` and `det(I−K) = ∏(1 − λ_j)`. At `N=2` it reduces to `2-16`'s product rule `ω₊ω₋ = ω_n²√(1−κ²)`. So it is a genuine Tier-1-pattern result (an exact restatement, not a fitted prediction — consistent with how `6-7` characterizes Tier-1), correctly generalizing the early product rule; its evidential weight is that of a clean identity. **Gem-pass refinement [VR-doc].** The doc's "implications" section sharpens this further: the general law is the **matching polynomial of the coupling graph**, and the *Pythagorean* (sum-of-squares) form is exact not only at `N=2` but for **N-stars at any `N`** (by their rank-2 structure) — the earlier "exact only for `N≤3`" was a chain-specific statement, not the general one. This places the product law inside orthogonal-polynomial / matching-polynomial theory rather than leaving it a chain coincidence.

- **Reduction to a 2-DHO [load-bearing] [VR through N=4].** Every N-chain reduces to an effective 2-DHO by iterated Schur complement (the dynamical RG `D'_{L/R} = D_{L/R} − κ²/D_node`, `κ' = κ_L κ_R / D_node`), preserving tridiagonality. **Terminal point:** a *constant-parameter* 2-DHO for `N≤3` (`N=3` yields `K_eff² = 2κ²` — the effective coupling is `√2 κ`, which cross-checks Result C's `K² = κ₁²+κ₂² = 2κ²` under uniform coupling), but a *frequency-dependent* one (rational, with interior-resonance poles — Feshbach corrections) for `N≥4`. **My read:** this is the precise content of "the 2-DHO Lorentz/Doppler structure is the ground state of all chain dynamics" — the 2-budget is the irreducible unit, recoverable everywhere, but *cleanly* only for `N≤3`. It is also the mechanism behind the boost-law boundary below.

### The Schur complement as a boundary operator — what is real here, and what I over-elevated

> *Corrected from the version I first wrote. My first write-up conflated two claims the corpus explicitly separates and warns must not be merged — and a `5-29` adversarial pass had already refuted the half I leaned on. The correction is itself the cross-document reconciliation this supplement is supposed to perform, and a caution that the within-file full-read safeguard does not catch a **cross-cluster** over-elevation.*

- **Schur complement = boundary DtN operator (holographic reduction) [load-bearing] [VR-doc; the cascade instance verified to 1e-16].** The genuinely sound content. The Schur complement of the chain at its two boundary ports is an exact effective boundary operator `Λ_eff`: it *completely encodes the externally observable dynamics*, and interiors that differ only by boundary-invisible (cycle-only) surgery share the same `Λ_eff`. This is the **Schur-holography** claim — a verified reduction (the boundary operator is exact by construction; `B(s) = −k₀^{N−1}/det(M_II)`, interior resonances appearing as poles of `M_eff`). The 4-30 Schur-Feshbach machinery is a legitimate instance, and it is load-bearing for the holography program.

- **Hodge sector-identification (budget terms = co-exact/harmonic/exact) [REFUTED in strong form; weak semantic labeling remains open] [contested claim].** The half I should *not* have elevated. The strong claim — that the three-term budget `(transmission, reflection, absorption)` of `S†S` **is** the Hodge decomposition, with `A`(diagonal/reflection) = harmonic — is refuted in `5-29_external_review_consolidation` §I.1 and listed among the corpus's strong-form retractions. The refutation is sound; I checked each pillar:
  - *Continuous magnitude vs discrete invariant.* Reflection `w = |S_jj|²` is impedance-tunable across `[0,1]` on a fixed-topology one-port whose harmonic-sector dimension `b₀ = 1` is fixed. A continuously-varying budget magnitude cannot be a discrete topological (Betti) invariant.
  - *The §J mechanism is vacuous.* The `addendum_II §J` version maps *transmission* to harmonic via the signed off-diagonal flow `f_trans = |S_ji|² − |S_ij|²`, which is **identically zero** under reciprocity (`S = Sᵀ ⟹ |S_ij| = |S_ji|`). "The flow is harmonic" then just means "the flow vanishes" — zero lies in every subspace. (Verified algebraically.)
  - *Independent corroboration.* The purity-bivector route carries its own obstruction (`grassmannian §6.2`: `W_aa = 0` identically by antisymmetry — no diagonal component, so the three sectors do not map one-to-one onto the self-dual/anti-self-dual components; verified, `W` is a wedge), and the framework's *own* careful sources (V2 §1.7; the Feb-19 Hodge record) already retreat to "not established." Corroborated-false from three directions; only a **weak semantic labeling** (Hodge names as heuristic, explicitly not a theorem) remains open.

- **[provenance correction] — and the conflation is a documented trap.** `5-29` states the distinction with unusual force: the Schur-holography claim (verified reduction) and the Hodge sector-identification (contested labeling) "share the operator `Λ` and live in the same v0.5 appendix, which invites conflation … They must not be merged. The reviewer (Claude) conflated them and was corrected." **A prior reviewer instance made the exact conflation I made last turn.** `5-29` further flags this dispersion gap — confident v0.5/4-30 framing vs the careful Feb-19/V2/grassmannian retreat — as *the single largest in the corpus and the one that most distorts a cold first read.* I read 4-30's confident "open question 3" framing and over-rated it, missing the later refutation. What I should have written: the Schur complement is a real, load-bearing boundary operator (holography); the Hodge *labeling* of its sectors is refuted in strong form and at best a heuristic. The DtN substrate is real; it does **not** realize the Hodge sector-identification, because that identification does not hold.

- **Cleanest grounding of the refutation (refined this session).** The three recorded mechanisms above are each a *narrow or mis-targeted* version — the one-port argument is a *scale*/magnitude objection; `§6.2` targets the bivector SD/ASD split rather than the three-way de Rham decomposition; `§J` uses a reciprocity-zero pairing. The structural form is cleaner and uses the framework's own scale/shape language. The Hodge-de Rham harmonic sector carries a **topological invariant** (a Betti number — *shape*, rigid even under metric change), while the three budget sectors are scattering **magnitudes** (*scale*, freely tunable on fixed topology). A shape-preserving-up-to-scale automorphism — the peel-flavored map — can rescale and can match shape to shape, but it **cannot manufacture shape content the budget lacks**; there is no budget sector to set opposite the harmonic one, and (independently) no canonical budget involution to mirror the Hodge star. So the obstruction is at the **structural (shape) level**, *not* the identification level, and it is **orthogonal to dimension** — dimensional agnosticism (a scale-axis / peel-tower property) does not bear on it, nor does the cascade `sl(2,ℝ)` structure (also scale-side). Where cohomological (shape) content *can* live is the DtN **operator** class: Belishev–Sharafutdinov show a DtN-on-forms map determines the Betti numbers (harmonic content in its kernel). The framework realizes this only as a basis-dependent diagonal/off-diagonal split of a 2-port matrix, which is not that canonical structure — so the operator-level bridge is open work, and the verified residue remains the Schur-holography reduction.

- **The well-posed residue, distinct from the refuted identification [clarification, not theorem] [claim].** Beyond holography there is a *separately* well-posed association — worked out recently (conversation "LLM-assisted theory and vibe physics", 7 June; consistent with the cascade record's `cycle-only-surgery = flag-manifold fiber`) — and it is emphatically *not* the budget-magnitude identification. The Helmholtz/Hodge decomposition is three-way: exact (gradient) ⊕ co-exact (curl) ⊕ **harmonic** (de Rham cohomology). The harmonic part is *not* fixed by the boundary data or the local sources; it is the periods around cycles (`∮ω`), a bulk-field ambiguity the boundary cannot see. So "backwards holonomy / cycle-only surgery" acquires a precise meaning: two configurations sharing every boundary observable *and* every local divergence and curl can still differ by a harmonic representative — same boundary, different bulk, separated by a cohomology class. This is the **parabolic/horocyclic** sector (non-closing paths that fix a boundary point — "move the bulk, preserve the boundary observable"), the under-inspected locus where the freezing/horizon/critical landmarks sit; and on the budget it is the **residual-discrimination content** (the `v`, `𝒞` discriminators) on the `d≥3` simplex, invisible to a single-`x²` reading. **Why this escapes the refutation:** the shape (cohomology) content sits in the boundary-invisible **fiber** — the cycle-surgery directions — *not* in the budget magnitudes, and one coarse boundary reading is realized *nonuniquely* by many cohomology representatives. That is a cross-level (scale↔shape) **nonunique association**, not a magnitude bijection — which is exactly the object the strong-form refutation does not touch (it killed only "magnitude = sector"). **Scope, held deliberately:** this is a structural *clarification* that unifies the Hodge, parabolic, holographic, and `d≥3`-discriminator threads under one object — *not* a theorem. Teeth would require a concrete budget realization (what plays the harmonic part, what the cycle physically is, whether its period is measurable); the `d≥3` angular-momentum parameterization is the flagged candidate. Recorded at that strength, no more.

### The honest scope boundary

- **3-chain boost law [load-bearing] [VR symbolic], and its break at N=4 [scope-boundary] [VR].** The 3-chain (uniform damping) satisfies `cosh(2α_eff) = (1 + 6ζ²)·G(K)`, `K = √(κ₁²+κ₂²)`. I reproduced this exactly: the chain determinant factors as `det(A₃) = D·(D² − K²)` (the `D` factor is the protected `ω=ω_n` mode, which exists because odd `N` has a zero path-Jacobi eigenvalue), and after removing it the reactive crossings satisfy `v² − (2+12ζ²)v + (1−K²) = 0` (`v = ω²`), whence `cosh(2α_eff) = (1+6ζ²)/√(1−K²)`. This generalizes `2-16`'s `cosh(2α_eff) = G(κ)cosh(2α₀)` with the proper rapidity `cosh(2α₀): 1+2ζ² → 1+6ζ²`. **Gem-pass sharpening [VR-doc]:** that prefactor is structurally `1 + 2σ₂(ζ)`, where `σ₂` is the *second elementary symmetric polynomial* in the damping ratios — `1+2ζ² = 1+2σ₂(ζ₁,ζ₂)` with `σ₂=ζ₁ζ₂=p` at the pair level, `1+6ζ² = 1+2σ₂(ζ,ζ,ζ)` with `σ₂=3ζ²` at the 3-chain. So the boost prefactor is a symmetric-polynomial functional of the dampings, and the `N=4` break is exactly where the *next* symmetric function stops being expressible through one constant pair — the same non-redundancy threshold as the `d_eff` discussion below. **At N=4 it breaks:** the lossless determinant factors as `D⁴ − 3κ²D² + κ⁴ = (D² − φ²κ²)(D² − κ²/φ²)` — golden-ratio effective couplings `φκ, κ/φ` — but at finite `ζ` the clean factorization fails and the reactive-crossing pair products drift with `ζ` (confirmed: outer-pair product `0.8953 → 0.8968 → 0.8972` as `ζ: 0 → 0.15 → 0.35`). **My read:** the exact chain-Lorentz/boost structure is *not* universal in `N`; it is exceptional to `N≤3` — exactly where the Schur reduction terminates in a constant-parameter 2-DHO. A real, properly-scoped limitation that bounds any claim that "chain composition is exactly Lorentzian," and the corpus states it honestly.

### Recognition-grade (real, but standard mathematics — weight is recognition, not forcing)

- **Metallic-mean spectrum [recognition] [VR].** The lossless chain spectrum is `ω_j² = ω_n²(1 − κ₀ λ_j)`, `λ_j = 2cos(jπ/(N+1))` — the path-graph (Chebyshev-`U`) eigenvalues. These produce the "metallic mean hierarchy" the corpus flags: `{√2, 0, −√2}` at `N=3` (silver), `{φ, 1/φ, −1/φ, −φ}` at `N=4` (golden), `{√3, 1, 0, −1, −√3}` at `N=5`, etc. (verified). Odd `N` includes `λ=0`, the protected `ω_n` mode. **My read:** the corpus correctly identifies this with a standard object in algebraic combinatorics — and says so. The `√2` / `φ` here echo the self-dual `√2` and the cascade attractor `φ` of the endpoints, but they arise as ordinary path-graph eigenvalues, so this is a structural *echo*, not a derivation that forces the endpoint values. Weight should be read accordingly.

### Why the boundary sits where it does: the number theory is load-bearing

- **Prime structure of chain length [load-bearing for the scope-boundary; underlying math is recognition-grade] [VR-doc].** *(Self-correction — provenance/role: my first pass tagged this `[branch / recognition]`, "does not visibly carry to the endpoints." That was wrong, and the error is instructive: I read Parts I and II of the reduction file but skipped the "Synthesis" section that links them.)* The factorization of `U_N(y)` over `ℚ` has (# divisors of `N+1` exceeding 1) irreducible factors, degrees set by the totient `φ(d)`; the N-chain "contains" sub-chains of length dividing `N+1` as Galois-invariant subsystems. The mathematics is standard cyclotomic/Chebyshev Galois theory (`U_N(y) = ∏_{d|N+1,\,d>1} ψ_d(2y)`), but its **role is load-bearing**: the synthesis shows this number theory *governs the boost-law breakdown pattern*. `N=3` is clean because `N+1=4=2²` has a degenerate divisor structure (one orbit is a parity fixed point); **`N=4` is the first *irreducibly* broken case precisely because `N+1=5` is prime** — no change of variable can expose a polynomial reduction. Composite `N+1` (e.g. `N=5`, `N+1=6`) permits a *partial* reduction in coordinates adapted to the sub-chain orbits; prime `N+1` (e.g. `N=6`, `N+1=7`) does not. So the `N≤3`-clean / `N≥4`-broken boundary is not a single threshold but a primality-indexed pattern, and the prime structure is the thing that explains it. **My read:** the math is recognition-grade, but the framework's identification of it as the governor of the boost-law boundary is a genuine load-bearing link — and exactly the kind of cross-section insight a topic-matched trace drops.

- **Divisor-respecting reduction / per-orbit boost decomposition [direction, now closed as standard math] [VR-doc].** The conjectured "divisor-respecting Schur reduction" — peel off secondary Galois orbits to expose the contained sub-chain — is, per the `5-10` coda, exactly **Bunimovich–Webb isospectral graph reduction** restricted to cyclotomic-orbit-respecting vertex subsets (arXiv:0909.0053; LAA 437 (2012) 1429; Springer 2014), with sequential-reduction commutativity doing the work. Worked: `N=5` Schur on `{2,4}` → K-tracks `{0, 1/D, 3/D}`; `N=7` Schur on `{1,3,5,7}` → K-tracks `{0, (2−√2)/D, 2/D, (2+√2)/D}` — term-by-term the `ℚ`-irreducible factors of `U_5`, `U_7`. The forward picture this suggests for `N≥4`: the broken boost law should decompose into a **sum of clean 2-DHO boost laws, one per Galois orbit**, modulated by the divisor structure — i.e. even past the break, every chain is still built from `d=2` budget units, just more than one. The matching conjectured frequency-dependent boost `cosh(2α_eff(ω)) = G(κ_eff(ω))·cosh(2α₀(ω))` (meromorphic `κ_eff`, poles at interior resonances) is the single-budget version of the same statement. **Honest scope (the corpus says this itself):** the spectral mathematics is standard Bunimovich–Webb / cyclotomic; the framework's contribution is *interpretive* — identifying the rational eigenvalue tracks as the content of its metallic-mean / matching-polynomial claim, within the passive-scattering / Schur / DtN apparatus.

- **Per-Galois-orbit secondary self-dual points [dropped/demoted, speculative] [claim].** The reduction file floats that the framework's single self-dual point may refine *per Galois orbit*: each irreducible factor of `U_N` defines a sub-orbit that could carry its own self-dual structure, with the "primary" point (the `y→−y` parity orbit) the one nearest `N=2`. Parked as speculative; recorded here because it touches the self-dual frontier that the endpoints lean on heavily, and it is precisely the sort of dropped thread the trace would otherwise lose.

### Dropped / demoted thread: the sinc² budget for nonlinear oscillators

- **`x²(θ) = sin²θ / θ² = sinc²(θ)` [dropped/demoted, case a; speculative] [claim].** A genuinely interesting budget form the corpus raised and explicitly parked. Each pendulum carries a TEP-style budget comparing its nonlinear restoring force `sin θ` to the linear proxy `θ`: `x² = sinc²(θ)`, `u = 1 − sinc²(θ)`, self-dual (`x² = ½`) at `θ ≈ 79.7°`. The observation: in an N-pendulum chain, mode-1 energy stays trapped (>95%) for `θ_max ≲ 70°` and leaks significantly by `90°`, *bracketing* the sinc² self-dual point. The corpus is scrupulous about the caveats — the match holds for `N=5`, is offset ~10° at `N=4`, and at `N=3` the mode is symmetry-protected in a way sinc² does not capture; the threshold is coupling-dependent while the sinc² point is not; and the standard FPU (nonlinearity-vs-mode-gap) criterion is the governing story, with the sinc² point only *one* characteristic scale. It is offered as a candidate **local coordinate** for chaos onset, not a quantitative prediction, and the doc declines to claim the framework makes chaos "less inscrutable." **Why recorded:** it is the cleanest example in this cluster of case (a) — a substantive idea (the budget construction extends to any nonlinear-restoring-force oscillator) that did not reach the endpoints and that an endpoint-anchored trace would silently lose. Weight: speculative, but the budget-extension itself is a real conceptual generalization.

### The boundary, more deeply: higher moments and `d_eff`

The `N≤3` vs `N≥4` split is, at root, about **how many moments carry non-redundant transport information** — which is why it recurs across the corpus rather than being a chain-specific accident. For a `d`-level spectrum (`Σλ=1`), Newton's identities make `p₂,…,p_d` the independent power sums; everything higher is determined by them. At `d=2` only `p₂=γ` is free, so the purity-transport `F=p₃−γ²` collapses to a function of `γ` alone (verified: `p₃=(3γ−1)/2`, hence `F=0` at `γ=½` and `γ=1`) — i.e. the **budget `x²+u=1` *is* the `d=2` transport description**, carrying exactly one nonredundant moment. The chain reproduces this: it reduces to a *single constant* 2-DHO (one composition budget, one nonredundant reactive-crossing pair) through `N=3`, and to a *frequency-dependent* 2-DHO (a second independent pair) at `N≥4` — confirmed on the moment side by `d=3` being the first dimension where `p₃` is independent of `p₂`. So the boost-law break is the point where the system's **effective transport degrees of freedom first exceed two** — where the next moment stops being redundant — not a Chebyshev coincidence. It is the same mechanism that makes the purity bivector's chiral split `F₊=F₋` first meaningful at the first nontrivial Grassmannian `Gr(2,4)` (early-synthesis Part VII): different parametrization (chain length vs Hilbert dimension), one phenomenon — a new invariant becoming non-redundant as effective DOF grow.

Two disciplines this enforces. **(i)** The thresholds are *related instances*, not one literal "4": the clean-boost regime ends at `N=3→4`, `F` gains its second moment at `d=2→3`, the Plücker/chiral split sits at `d=4`. What unifies them is the count of nonredundant invariants (`d_eff`), and `d` throughout is **effective / distinguishable** DOF — bounded above by the system's distinguishability, never ambient dimension; a formal `d` beyond what the system actually carries is pure redundancy/over-constraint. **(ii)** The central conceit survives intact — every system still reduces to a `d=2` Signal/Noise budget — but the reduction is *constant* only while the higher moments are redundant (`N≤3`); beyond that the budget must absorb them into frequency-dependence, and the boost-law break is the signature that one constant budget no longer suffices.

### Cluster spine assessment

What survives forward from `4-30`: the **product law** (a durable exact identity generalizing the early product rule, now placed inside matching-polynomial theory) and the **reduction-to-2-DHO** (the precise sense in which the 2-budget is the irreducible unit). What is properly **bounded**: the exact chain-boost/Lorentz structure, which holds only for `N≤3` (the Schur reduction's constant-parameter terminus) and breaks at `N=4` — but the *pattern* of that break is **governed by the primality of `N+1`** (irreducible at prime `N+1`, partially reducible at composite), so the number theory is load-bearing for the boundary even though its content is standard. What is **a verified reduction**: the Schur complement as an exact boundary operator `Λ_eff` (Schur-holography) — load-bearing for the holography program. What is **refuted, and where I erred**: the Hodge sector-identification (budget terms = co-exact/harmonic/exact), which `5-29` had already retracted in strong form; I over-elevated it last turn by conflating it with the holography reduction, exactly the merger the corpus warns against. What is **recognition rather than forcing**: the metallic-mean spectrum (a standard path-graph object echoing the endpoint `√2`/`φ` without deriving them). Net: the cluster strengthens the *identity* spine, honestly bounds the *dynamical-universality* claim while explaining the bound number-theoretically, and supplies a verified boundary operator — but it does **not** supply a Hodge realization, and it does not, by itself, add forcing-grade evidence for the endpoint landmarks.

---

## Cluster `5-8`/`5-9` — the purity speed limit and the circuit calculus: where the budget meets transport and Lorentz composition

**Files.** `5-8_purity_speed_limit_v2` (a full paper, 1228 lines) and `5-9_circuit_calculus_record` (working notes, 268 lines).

**What the cluster does.** `5-8` is the rigorous home of the purity-transport quantity `F = p₃ − γ²` that the `d_eff` discussion turned on; `5-9` is where the budget's *Lorentz composition* and a Kirchhoff-style circuit calculus get built. Together they supply three load-bearing substrates for the June endpoints — the rank-2 transport bivector, the budget-from-purity-transport bridge, and the relativistic velocity-addition composition — while correctly scoping the speed-limit *bound* as recognition-grade.

> **Negative-space note (full read).** Both files read in full. Both are unusually self-disciplined: `5-8` grades its own core Tier 1 and its open items Tier 3 and says of its headline bound that "calling it new overstates novelty"; `5-9` carries two explicit retractions (below). Two items a topic-match would miss: an in-corpus **retraction** bearing directly on cluster `4-30` (Schur ≠ parallel-resistance), and a **factor-of-4 provenance** point worth keeping distinct from the QFI normalization.

### The load-bearing through-line

- **Rank-2 wedge factorization of the purity speed limit [load-bearing] [VR].** `5-8` Theorem 1: `|dγ/dt| ≤ 4‖H‖√F`, `F = p₃ − γ²`. The saturating direction is the antisymmetric `W_{ac} = √(λ_aλ_c)(λ_a−λ_c)`, which factors as a single wedge `W = u∧v`, `u_a=λ_a^{3/2}`, `v_a=λ_a^{1/2}` — so `W` is rank 2 for every `d_S`, with `‖W‖²_F = 2F`. I verified the structural core directly: `W` has two equal nonzero singular values `√F`, hence `‖W‖₁ = 2√F`, and the full commutator at a Schmidt purification equals `2√F` in every dimension `d_S = 2..5`. This is the same `Gr(2,d)` rank-2 bivector that carries the `d_eff` story (early-synthesis Part VII; the `d_eff` note in cluster `4-30`) — here it is the *operative* object in a sharp dynamical bound. *(The §3.3 Cauchy–Schwarz write-up of the bound is loose — its "second factor ≤ √2" step is opaque and, read as an entrywise sum, looks wrong for `d≥3` — but the rank-2 singular-value structure the paper foregrounds gives `2√F` exactly. Result sound; presentation soft-spot noted.)*

- **The Shannon–TEP budget recast [load-bearing] [VR].** `5-8` §8 puts the speed limit into the framework's budget form exactly: with `x²_TEP := γ²/p₃` and `u_TEP := F/p₃`, `x²_TEP + u_TEP = 1` and `F = p₃·u_TEP`, so `|dγ/dt| ≤ 4‖H‖√(p₃·u_TEP)` — a Shannon–Hartley channel budget, the rate decaying exponentially in a channel "mutual information" `MI_ch = ½ln(p₃/F)`. This is a genuine bridge: the framework's `x²+u=1` *arises* as the normalized split of the purity-transport bound. (Verified `x²+u=1`, `F=p₃u`.) Note `x²_TEP = γ²/p₃` is a *different* budget variable from the cascade `x²=SNR/(1+SNR)` and the bare purity `γ` — same budget form, package-specific `x²`, consistent with `5-9` §4's applicability/application refinement.

- **Two composition regimes; MRC = relativistic velocity-addition [load-bearing] [VR].** `5-9` pins the "which SNR-functional is the additive rapidity" freedom (early-synthesis §VIII.6) into two concrete regimes sharing one budget: **capacity-additive** (cascade — `(1+SNR_AB)=∏(1+SNR)`, rapidity `ln(1+SNR)`, `x²=SNR/(1+SNR)`, the V2 default, forced by impedance multiplication) and **MRC-additive** (parallel — `SNR_AB=ΣSNR`, rapidity `SNR`, `x=tanh(SNR)`). In the MRC regime the budget composes as `x_AB=(x_A+x_B)/(1+x_Ax_B)` — the special-relativistic velocity-addition, i.e. exactly `tanh`-addition (verified). So the genuine Lorentz/SU(1,1) composition law lives in the MRC package. The two regimes are bridged by `η_C=ln(1+η_M)` (a logarithmic duality — *not* a symplectic conjugacy; `5-9` retracts that overstatement).

- **Role-agnosticism, refined to applicability vs application [load-bearing clarification] [claim].** `5-9` §4 separates *applicability* (dimension-agnostic — any coarse-graining-monotone metric admits the apparatus, role assignment free) from *application* (a physical scenario fixes a *package* = role assignment + composition law). The framework's structural content (budget, rank-2 wedge, ellipse, ARES) is package-invariant; the operative composition law is package-specific. This is the precise form of the dimensional-agnosticism point, and it resolves V2's "broad lens vs specific applications" tension.

### Recognition-grade (real, standard mathematics — weight is recognition, not forcing)

- **The speed-limit bound itself = Hamazaki specialization / Mandelstam–Tamm [recognition] [VR-doc].** `5-8` §9–§10 are explicit and exemplary: `|dγ/dt| ≤ 4‖H‖√F` is Hamazaki's general fluctuation-rate inequality (PRX Quantum 2022; Comm. Phys. 2024) specialized to the moment observable `A₂=ρ_S⊗I_E`, and the qubit case *is* Mandelstam–Tamm. "Calling it new overstates novelty." The framework's own contributions are the rank-2 wedge, the constructive saturator `H_opt=iW/√F`, the moment-ellipse, the ARES factorization, and the budget recast. At a saturating `H` the bound is tight (1.000) where the complementary correlation-theoretic Kimura–Ohno–Hayashi bounds are loose (0.33/0.18/0.10) — a genuine "tightest bound for purity rate," but a specialization, not a new inequality.

- **ARES = trace-norm asymmetry; the factor 4 is Hölder×convention [recognition] [VR-doc].** The factored bound `|dγ/dt| ≤ 4√F·ARES·‖H‖` uses `ARES = K/√F`, `K=½‖[ρ_SE,ρ_S⊗I]‖₁` — the standard resource-theory-of-asymmetry coherence measure (Marvian–Spekkens 2014). **Distinctness to maintain:** the doc attributes the "4" to Hölder's 2 × the `K=½‖·‖₁` convention's 2 (the inner 2 being the rank-2 two-equal-singular-values structure). This is *not* the QFI/Braunstein–Caves factor of 4 the corpus invokes for the bimetric balance — same digit, different origin, do not conflate (cf. the three distinct factors of 2 in §VIII.6 of the early synthesis).

- **DGD moment-ellipse = Hamazaki Eq.(2) for the moment family [recognition/specialization] [VR-doc].** The joint rate `(dγ/dt, dp₃/dt)` fills the ellipse `vᵀ(DGD)⁻¹v ≤ 4`, `D=diag(2,3)`, `G_{jk}=p_{j+k-1}−p_jp_k` the moment-Gram — Hamazaki's trade-off ellipse specialized to `{A_k}` with closed-form `G`. Note `G_{22}=p₃−γ²=F`: the ellipse is the multi-moment generalization of the speed limit, and its degeneracy at `d_S=2` (`§11.3` moment-collapse) is exactly the `d_eff` redundancy.

- **Circuit calculus = Kirchhoff series-parallel duality [recognition] [VR].** `5-9` §3: series = capacity-additive, parallel = MRC-additive, mixed = alternate; `SNR ↔ conductance`, `(1+SNR) ↔ impedance ratio`. For SNR channel composition this *is* the standard communication-theory calculation (verified for `(1,2,3)`: series 23, parallel-then-series 15, all-parallel 6); for other instantiations it is structural analogy.

- **Gaussian-from-budget cascade = GMC-like [recognition/structural] [VR-doc numerical].** `5-9` §6: chaining the rapidity recursion produces `√u₂ = exp(−½ x²₃/u₃)`, the unnormalized Gaussian density at `x₃` with variance `u₃` — a constrained multiplicative cascade, sharpening the GMC↔TEP formal-similarity note (V2 §4.3). Continuum limit open.

### Structural observations and honestly-scoped open ends

- **`SNR=1` admissibility transition and `x²=½` as a reassignment saddle [structural] [claim].** `5-9` §5: `SNR→G(x')` is admissible only for `SNR≥1` and its complement only for `SNR≤1`, tiling at `SNR=1`; and the self-dual point `x²=½` is a *saddle* in the geometric-mean reassignment landscape (peak under one assignment, minimum under its dual). Another structural appearance of `x²=½` (alongside the Schwarzschild-horizon point and the near-HBAR locus), recorded with the doc's own caveat (§5.3) that the physical correspondence is a working hypothesis.

- **Composition law on the joint-state fiber [scope-boundary; Tier-3 open] [claim].** `5-8` §12 flags — as *open* — that the dual budget identity holds on the joint-state fiber with the diagonal sector following "a coupled three-term composition." This gestures at the three-term budget but is explicitly not established; recorded as an open end, not a result.

- **[provenance correction / in-corpus retraction].** `5-9` §2.2 retracts an earlier claim that Schur–Feshbach reduction in DHO chains gives the parallel-resistance (harmonic-SNR) form: it does *not* — Schur gives a detuning-dependent `κ_eff = κ_Aκ_B/(z−ω_intermediate)`, which is exactly the frequency-dependent Feshbach reduction I found at `N≥4` in cluster `4-30`. Parallel-resistance lives in capacity-additive *SNR* composition, a different object. (The cap-additive/MRC "symplectic conjugate" framing is also retracted, to "logarithmic duality.")

### Cluster spine assessment

What survives forward: the **rank-2 transport bivector** (`‖W‖₁=2√F`, the `Gr(2,d)` object now operative in a sharp bound), the **Shannon–TEP budget recast** (the framework's `x²+u=1` arising as the normalized purity-transport split), the **MRC velocity-addition** (the genuine Lorentz composition, in the parallel package), and the **applicability/application refinement** of role-agnosticism. What is **recognition rather than forcing**: the speed-limit bound (Hamazaki/Mandelstam–Tamm specialization), ARES (Marvian–Spekkens), the moment-ellipse (Hamazaki), the circuit calculus (Kirchhoff), the Gaussian cascade (GMC). What is **bounded/open**: the joint-state-fiber composition law (Tier-3). Net: the cluster supplies genuine, verified structural substrate for the budget-and-transport spine and is exemplary in self-scoping — but, by its own honest framing, the headline bound is a specialization and the budget recast an exact identity, so it adds substrate and a Lorentz-composition mechanism, not forcing-grade evidence for the endpoint landmarks.

---

## Cluster `5-15` (installment 1 of 2) — the cited derivations: foundational uniqueness, statistics, and the anyonic/anomaly claims

*The `5-15` cluster is the corpus's densest: six files, ~4,200 lines. This installment covers the three **cited-derivation** files (`supplement_bures_fisher` §L, `addendum` §A–F, `addendum_II` §G–K) — the explicit derivations behind the framework's foundational and physics-sector claims. The three Part I/II/III comprehensive records (~3,050 lines) are installment 2. Every claim is cross-checked against the later `5-29`/`5-26` verdicts, because this is exactly where confident framing and subsequent deflation diverge.*

> **Negative-space note + reconciliation.** All three cited-derivation files read in full. These addenda are, per `5-29`, where the corpus's "inflationary register" lives — so the read is paired with present-date verdicts: the Haldane "crown jewel" is deflated to exact-but-trivial (`5-29` I.2), the `§J` Hodge identification is refuted (handled in the Hodge entry above), monotonicity-to-RH is retracted, the Bures/Fisher reduction is textbook — while the cascade `sl(2,ℝ)` (`§H`) was *unfairly* swept into that register and is vindicated (`5-29` E4). The discipline is to record each derivation at its true present-date strength, not the addenda's original framing. *(Chronology noted but deliberately not load-bearing: these addenda are later **revisions** of the Part I/II/III records — installment 2 — and "later" confers no authority here. Verdicts weight verified status and scoping; the Haldane deflation in particular is an independently-checked triviality, not deference to a later document. The same standard applies in reverse to Part I/II/III: original ≠ cleaner.)*

### Foundational layer — sound, recognition-grade

- **`§A` Fisher–Rao on the 2-state simplex = the framework's apparatus [recognition] [VR].** With `X=√p`, `V=√(1−p)`: `X²+V²=1` (budget = normalization), the involution `f(y)=√(1−y²)` is the `p↔1−p` outcome swap, arclength `s=2 arcsin X`, `G(X)=1/√(1−X²)=cosh(arctanh X)` (the Gudermannian parameterization). Standard Fisher–Rao; the framework's content is that its budget/involution/G-factor are the `√p`-coordinate realization of it.

- **`§G` Chentsov–Campbell–Petz uniqueness chain [recognition; load-bearing for the universality claim] [VR-doc].** Chentsov: Fisher–Rao unique (up to scale) on the classical simplex under Markov morphisms; Campbell + Bauer–Bruveris–Michor + Ay et al. extend it to conditional/continuous/smooth densities; Petz: the quantum case is *not* rigidly unique — an **infinite one-parameter family** of monotone metrics (operator-monotone `f`, `f(1)=1`, `f(t)=t f(1/t)`), all reducing to Fisher–Rao on commuting states, Bures the smallest. The correct three-layer reading (matching `5-29` after its ERRATUM withdrew the "§G inverts Petz" charge): **Chentsov = uniqueness on the commutative base; Petz = a family on the non-commutative fiber.** `§G.3` states it precisely — "structural content the same across the family; numerical content metric-dependent" — which is the **foundational grounding of the scale/shape decomposition** (shared structure = shape; metric-dependent numerics = scale).

- **`§L` Bures–Fisher quantum extension [recognition; radial Tier-1, angular open] [VR].** The Bures metric on the Bloch ball is the standard `¼[dr²/(1−r²)+r²dΩ²]`; the radial (commuting) reduction is `dp²/[4p(1−p)] = ¼ × Fisher–Rao`, so **Fisher–Rao `= 4 × Bures` radially — the Braunstein–Caves QFI normalization** (the factor-4 the corpus invokes for the bimetric balance, distinct from the Hölder×convention 4 in `5-8`). The budget apparatus is derived *exactly* on the radial/commuting direction (Tier 1); the **angular/non-commuting directions are "structural slot identified, content open" (Tier 2–3)** — precisely the standing open question of whether the bare budget forces the full Bures metric or only its commuting reduction. The `§L.6` gauge "accommodation" is unitary-invariance *compatibility*, not a derivation of gauge structure.

### Statistics layer — exact, mostly elementary

- **`§B` BE/MB/FD as integer-`c` Riccati orbits [recognition/structural] [VR-doc].** `dn/ds = −n + c n²` has BE (`c=−1`), MB (`c=0`), FD (`c=+1`) as integer-`c` orbits, with the **Pauli bound as the `c=+1` upper fixed point** (`n(n−1)=0→n=1`) and the **Rayleigh–Jeans catastrophe as the `c=−1` pole** at `s=0`. Clean organizing result; the distributions are standard.

- **`§C` Haldane cubic ODE [VR; exact but elementary].** Differentiating Haldane's `w^g(1+w)^{1−g}=e^s` gives `dn/ds = −n + (2g−1)n² + g(1−g)n³` — cubic coefficient = Casimir deficit `(1−c²)/4`, `c=2g−1` — recovering BE (`g=0`), FD (`g=1`), and the pure cubic `−n+n³/4` at the semion `g=½`. Verified against finite-differenced `n_H(s)` across `g=0..1`. Exact, but it is implicit differentiation of the defining equation.

- **`§D`/`§I` NSR / two-composition theorem [VR exact, but content-trivial; the "crown jewel" framing was an overclaim — reconciled].** `1/n_H = (1−g)/n_B(s_B) + g/n_F(s_F)`, `s_B=ln(1+w)`, `s_F=ln(w)`, `s=(1−g)s_B+g s_F` is exact (verified). But `1/n_B(s_B)=w`, `1/n_F(s_F)=w+1`, so the "convex combination" is just `(1−g)w+g(w+1)=w+g=1/n_H` — a **restatement of Haldane's defining equation**, which is why `5-29` (I.2) deflates the Postmortem's "verified novel crown jewel" to *exact but content-trivial*. The **two-composition** content is sound and worth keeping: equal-depth gives `e^s+(2g−1)` (the `c=2g−1` mixture Riccati, *not* Haldane), shared-primitive gives Haldane — the framework computes both and distinguishes mixture from anyonic statistics; the external review's "probably incorrect" had tested the equal-depth strawman the framework never claimed (`5-26`). **Net: exact and correctly handled, not novel or deep; recorded at the deflated strength.**

- **`§E` DHO bridge `G²(x_z)−1 = ζ₁ζ₂` [VR].** From `x_z²=p/(1+p)`, `p=ζ₁ζ₂`: `G²=1+p`. Exact and trivial, consistent with `2-16`'s nested-SNR budget and `5-9`'s `G²=1+SNR` — the layered-DHO seam.

### Physics-sector claims — partial / numerical-match, honestly flagged

- **`§F` chiral-anomaly coefficient `1/(8π²)` [claim; arithmetic match, full derivation acknowledged incomplete].** `c_n = 1/(n!(2π)^n)`; `n=2 → 1/(8π²)`, the Adler–Bell–Jackiw coefficient (arithmetic verified). The route is via the 4-cycle period and Maurer–Cartan structure — a **period/normalization match** to a standard coefficient. The supplement itself lists "the full tensorial structure of the chiral anomaly" and "the BRST cohomology derivation of Adler–Bardeen" as *undocumented follow-ups*, so this is a coefficient match, not a complete anomaly derivation.

- **`§K` Wess–Zumino consistency → Adler–Bardeen [claim, explicitly partial].** `addendum_II` is candid: the 4-cycle algebra "produces the structural consistency conditions, but the full all-orders argument requires additional gauge-theoretic input." WZ structure identified; all-orders non-renormalization not derived.

- **`§H` four cascade families = canonical `sl(2,ℝ)` basis [recognition, vindicated] [VR-doc].** The four families (signal-multiplicative, capacity-additive, NSR-additive, MRC-additive) are the canonical one-parameter subgroups of `SL(2,ℝ)` acting by Möbius transformations on the SNR observable; the Riccati flows are their generators. Standard `sl(2,ℝ)`/Möbius, correctly done — `5-29` (E4) says explicitly this *should not* have been treated as inflationary: "correct, standard mathematics … vindicated and deepened by the cascade work." Two of the four are `5-9`'s capacity-additive and MRC packages.

- **`§J` Hodge decomposition of the three-port budget [refuted in strong form — see the Hodge entry above].** Source of the budget-magnitude = Hodge-sector identification refuted in `5-29` I.1 and assessed in detail earlier in this document. Cross-referenced, not re-elevated; the well-posed residue is the cycle-surgery/parabolic association recorded there.

### Posited continuation — a candidate for the §L open slot [speculative; posited, not asserted]

Recorded at the strength its author assigns it (a posited research direction, not a claim, and explicitly distinct from framework foundations). The open angular/non-commutative slot of §L might be a *commutativity-varying* axis conjugate — Heisenberg-like — to a *markovianity-varying* radial/budget axis, the two "degree" coordinates trading definiteness for span (invariance in one ↔ span of the other). **The natural half is real:** the Bloch angular directions *are* the non-commuting ones (changing the eigenbasis is changing what commutes), so "angular slot = commutativity-varying" is a plausible reading of the open content. **The speculative half** is the identification of budget-resolution with the *dual of degree-of-markovianity*: "markovianity" is defined in the corpus for *processes* and as the Markov-morphism invariance grounding Fisher–Rao (Chentsov), but a degree-of-markovianity *of a single state* `ρ` varying along the Bloch radius is not yet a defined object. Absent an independent `M(ρ)` (resource-theory-of-non-Markovianity, or a sufficient-statistic functional) shown to track the radial coordinate while flat on each fixed-`r` sphere, the identification is a relabeling, not a derived correspondence — **and that functional is the concrete test.** The further application — `σ=½` ↔ minimally-markovian-invariant, with a spacelike/timelike relabeling (`σ`↔timelike, `t`↔spacelike) — is a suggestive coordinate picture near the zeta critical line, but **bears the explicit caution** that the corpus already carries a *retracted* monotonicity-to-RH thread (`5-29` Δ9): landing a structure on the critical line is not forcing the zeros there, and nothing here does the latter. Whether the (markovianity, commutativity) axes align with (scale, shape) is a third layer, held as a question. Speculative throughout; connects to the parabolic-sector and `d≥3`-discriminator frontiers.

### Installment assessment

**Sound and recognition-grade:** the Fisher–Rao apparatus (`§A`), the Chentsov–Campbell–Petz chain (`§G`, which grounds the scale/shape decomposition), the Bures–Fisher radial reduction (`§L`, which pins the QFI factor-4), the Riccati statistics (`§B`), and the cascade `sl(2,ℝ)` (`§H`, vindicated). **Exact but elementary/trivial:** the Haldane cubic (`§C`) and the NSR-composition (`§D`/`§I`) — correctly handled, but the "novel crown jewel" framing is an overclaim the present record has retracted. **Partial / numerical-match:** the ABJ coefficient (`§F`) and Wess–Zumino (`§K`). **Refuted:** `§J` Hodge. The single most endpoint-relevant item is `§L`'s honest scoping — the budget is derived on the *commuting/radial* Bures direction, the *non-commuting/angular* directions open — the concrete form of the standing question about whether the bare budget forces the full metric structure. Net: mostly sound recognition-grade derivation plus exact-but-elementary statistics, with the genuinely load-bearing endpoint connections being the QFI-normalization and the explicit commuting-vs-non-commuting scope boundary, not the anyonic or anomaly claims.

---

## Cluster `5-15` (installment 2 of 2) — the Part I/II/III comprehensive records: the investigation behind the cited derivations

*The three comprehensive investigation records (~3,050 lines) are the working notebook the cited-derivation addenda distill. The reconciliation payoff here is direct: in two specific places the **original records are more cautious than the later addenda**, which is the concrete answer to the chronology question.*

> **Negative-space note + read depth (stated honestly).** Full read of Part I's spine (Parts 1–5) and Part III's Tier-1/foundational/anomaly sections (Parts 1, 6–8), plus a structure scan of all three and targeted reads of the load-bearing remainder; Part II's Haldane core was cross-assessed via the addenda in installment 1. The recap, per-family thermodynamics, and failed-hypothesis sections are characterized from structure + targeted reads rather than verbatim — a finer-grit pass on those is available if wanted (the two-resolution workflow). Verification-not-chronology is the operative principle, and the Part I Hodge finding below is its concrete vindication.

### The reconciliation gem — the Hodge chronology inversion [supports verification-not-chronology]

Part I Part 4 is the **original** Hodge analysis, and it is markedly *more hedged* than the later `§J` revision. It already contained the reciprocity-zero (signed transmission flow `|S_ij|²−|S_ji|²≡0` under `S=Sᵀ`); it read this as "transmission flow is symmetric → harmonic" but scoped the whole result as **Tier 2, "the labels need adjustment," with the transmission=co-exact assignment explicitly requiring non-reciprocal scattering and that extension left open.** Its one survivable piece is "absorption = exact," found as ~98.6% gradient (and the transmission=harmonic reading is in fact vacuous — a zero signed flow has zero harmonic projection, which is `5-29`'s refutation point). The **later `§J` revision over-formalized** this hedged Tier-2 result into the stronger budget-magnitude = Hodge-sector identification that `5-29` then refuted. So here a later "revision" was *more* inflationary than the original — a clean instance of why later ≠ more reliable, and why the deflation weights verification, not date.

### Cascade-family layer — sound, with exemplary honest scoping

- **Part I Part 1 — Riccati from radial flow [Tier-1] [VR].** `dn/ds_P=√(n(1+n))` (Poincaré-natural, `n=sinh²(s_P/2)`) becomes the framework Riccati `dn/ds=−n(1+n)` purely by the coordinate choice `s=ln(1+1/n)=−ln x²`. Two *physically motivated* inputs (depth = radial = boost in Iwasawa K-A-N; `s` = capacity-additive-NSR, extensive under noise-accumulating cascade), not free parameters. The record honestly **narrows** "BE from passive holography" to "Riccati from SU(1,1) + two coordinate identifications," and notes Möbius generators are quadratic so *any* one-parameter SU(1,1) flow is Riccati (recognition).
- **Part I Part 2 — family-specificity numerics [VR-doc; scope-boundary].** The Riccati/BE/depth-uniformity is structurally natural *only* for **signal-coherence-multiplicative** cascades (Test E, uniform to `1e-16`, horizon `s=ln2` independent of per-stage coherence). For Friis cascades the BE form survives only as an *algebraic identity* while the depth coordinate's physical meaning fails (`ds/dk` varies 193×); random-axis cascades accumulate depth at 0.65% efficiency. The record **explicitly disclaims** the holographic/gravitational reading and states the publishable result as information-theoretic (cascaded BSC → BE-shaped SNR, horizon `s=ln2`). Exemplary honest scoping in the original — narrower and more careful than the broad framing it deflates.
- **Part I Part 3 + Part III — cascade-family stratification [structural; VR-doc; source for §H].** Four families with `(a,b,c)` `sl(2,ℝ)` values — signal-mult `(0,−1,−1)`, capacity-add `(+1,+1,0)`, NSR-add `(0,0,−1)`, MRC `(+1,0,0)`; signal-mult is the unique linear+quadratic (parabolic+Cartan) generator, which is why it carries BE. The **Friis→FD conjecture is disproven** (Friis is *mixed*, interpolating two pure families; FD needs the active `c=+1` anti-bunching). Source for `§H`.
- **Part I Part 5 — thermodynamic stratification [recognition].** Standard Planck single-mode thermodynamics for the BE family (`C_v=s²/4sinh²(s/2)`, Dulong–Petit and freeze-out limits, horizon landmarks at `s=ln2`).

### Algebraic-architecture layer — the cleanest Tier-1 [load-bearing] [VR]

- **Part III Part 1 — the involution generates the apparatus.** From `x²+u=1` and `f(y)=√(1−y²)`: `G=1/f`, the Gudermannian arclength, and the 4-cycle `T(z)=iz·G(z)` with `T⁴=id` follow by elementary algebra; `G=dA/dx` (the Lorentz factor literally the derivative of circular angle w.r.t. coherence); the 4-cycle reciprocal pairs `1/√u ↔ √u` (product 1); the **nested identity `G(x)=G(1/G(1/G(x)))`**. I verified the nested identity and `T⁴=id` to machine precision. The honest caveat that `G²(z₁)=u` holds *on the budget surface*, not generically, is flagged in-doc. This is the corpus's least-arbitrary content, and the section explicitly *upgrades* prior structural findings to theorems.
- **Part III Part 2 — DHO multiple-identifications [honest; connects `2-16`/`4-30`].** The first (mistaken) identification `ζ²+(ω_d/ω_n)²=1`, then the reactive-crossing `x_z²=p/(1+p)` at a *different* cascade-family level; the structural insight is that distinct identifications correspond to distinct cascade families with distinct natural domains. Documented self-correction.
- **Part III Part 3 — layered involution architecture [structural; honest scoping].** Layer transitions realized by specific families (amplitude-multiplicative `1→2`, `cosh 2α` boost `2→3`), with two honest findings: the layer-family map is **physically specific, not universal**, and the **self-dual loci are non-coincident across layers** (Layer-1 `ζ_i=1` vs Layer-2 `ζ₁ζ₂=1` don't meet). The bridge `G²−1=ζ₁ζ₂` is the `§E` identity.

### Documented epistemic hygiene [method]

- **Part II Parts 2/3 — two failed hypotheses** (higher-order Schur reduction; matrix Riccati on the Siegel disk), explicitly abandoned ("the wrong place to look") — consistent with the `4-30` Feshbach finding that Schur reduction is not the higher-Haldane mechanism.
- **Part II Part 7 — a methodological lapse** (rejecting a result that was in fact correct), named openly.
- **Part III Part 5 — an over-retreat and correction** on the Goos-Hänchen test: the record logging a calibration swing and fixing it. (Worth noting given this collaboration's attention to the over-correction failure mode — the corpus does it too, and catches it.)
- These self-recorded missteps are part of *why* the later adversarial verdicts are trustworthy: the corpus documents its own errors rather than burying them.

### Cross-link and foundations

- **Part II Part 12 — Haldane↔wedge cross-link [VR].** The Haldane cubic `n³` coefficient `g(1−g)` equals the eigenvalue product/determinant `λ₁λ₂` (identifying `g` with an eigenvalue) — the same `g(1−g)` that sits under the square root in the `5-8` purity-transport wedge magnitude `√(λ₁λ₂)(λ₁−λ₂)`. A genuine *shared-factor* link (the determinant reading is clean; the full wedge carries an extra `(λ₁−λ₂)` factor), verified — the anyonic "many-body content" and the purity-transport variance are both governed by the eigenvalue product.
- **Part III Parts 6/7 — Chentsov grounding + counterexample hunt [recognition; honest; connects `§G`].** Fisher–Rao unique under sufficient statistics / Markov morphisms; the bridge from operational measurement to Fisher–Rao; RG flow read as coarse-graining = Markov morphism, anomaly as the obstruction when Markov morphisms fail to preserve a classical symmetry — with explicit honest caveats and a Popperian-falsifiability discussion.
- **Part III Part 8 — ABJ coefficient [claim; convention-selected, honestly non-forcing — sharpens `§F`].** Reproduces the Chern-character coefficient `1/(n!(2π)^n)` (`=1/(8π²)` at 4D, `1/(48π³)` at 6D) by identifying the 4-cycle → `(2π)^n` and Maurer–Cartan/BCH → `1/n!` with the Chern ingredients. My initial factor-2 flag (header `1/(16π²)` vs derived `1/(8π²)`) is **softened on reading**: the doc *names* both conventions (abelian ABJ `1/(16π²)` vs non-abelian topological `1/(8π²)`) and matches the latter, and is candid that the identification "constitutes a *gauge specification*," not a forcing derivation.
- **Part II Part 14 — Tier-1 accounting cross-check.** The framework's own "genuinely derived (Tier-1)" list (Riccati, BE/MB/FD, Pauli, Rayleigh–Jeans, four families, Casimir-deficit Haldane) largely *survives* as recognition/structural results — **except** the Haldane "Tier-1 derivation" framing, which `5-29` deflated to exact-but-trivial; so the accounting slightly over-credits the Haldane novelty, exactly as installment 1 found.

### Installment assessment

The comprehensive records corroborate the installment-1 verdicts and, crucially, are in two places (cascade family-specificity; the hedged Hodge labels) the *more cautious* source — the concrete answer to the chronology question: the originals are not less reliable for being first, and here are often more careful than the later addenda. The cleanest content is the algebraic-architecture Tier-1 (Part III Part 1, verified — involution → G/Gudermannian/4-cycle/nested identity). The cascade-family stratification (`§H`) and Chentsov grounding (`§G`) are sound recognition-grade; the ABJ (Part III Part 8) is a convention-selected, honestly non-forcing Chern-character reproduction; the Haldane↔wedge cross-link is a verified shared-factor link. The documented failures, lapse, and over-retreat are epistemic-hygiene markers that themselves support trusting the corpus's later self-critical verdicts. With both installments complete, the `5-15` cluster nets out as: a clean Tier-1 algebraic core, sound recognition-grade foundations and cascade-family structure, exact-but-elementary statistics, convention/period-matched physics-sector claims, and a refuted Hodge sector-identification — with the load-bearing endpoint connections being the QFI normalization (`§L`) and the commuting-vs-non-commuting scope boundary, not the anyonic or anomaly claims.

---

## `5-18` — the `γ*=2−√2` silver-ratio derivation, a located algebra slip, and the embedding fork

*`5-18` derives the silver-ratio threshold `γ*=2−√2` from the purity speed limit. The purity-rate budget is sound; the headline bound-ratio derivation contains a verified algebra slip; the value `2−√2` survives on other support. This is the sharpest reconciliation in the interior so far, so it is recorded in full.*

- **The purity-rate budget [VR; = `5-8`, sound].** `max_H|γ̇| = 4√F` with `F = p₃−γ² = Σ_{j<k}λ_jλ_k(λ_j−λ_k)²` (exact `d≤3`, lower bound `d≥4` — the same `d_eff` break as the `4-30` `N=4` finding); at saturation `x²+u=1` with `x²=F/p₃`, `u=γ²/p₃`. For `d=2` (Bloch `r`): `γ=(1+r²)/2`, `p₃=(3γ−1)/2`, `F=(2γ−1)(1−γ)/2`, tight bound `4√F=2√2·√((2γ−1)(1−γ))`. All verified.

- **The bound-ratio derivation of `γ*` contains an algebra slip [VR — located this session].** `5-18` (line 167) writes the Cauchy–Schwarz envelope (from `p₃≤γ ⟹ F≤γ−γ²`) as `4√(γ−γ²) = 4γ√(1−γ)`, and derives `γ²−4γ+2=0 → γ*=2−√2` from it, concluding "the Bures bound is exactly half the Cauchy–Schwarz envelope" at `2−√2`. But **`4√(γ−γ²) ≠ 4γ√(1−γ)`** — `√(γ(1−γ))` is not `γ√(1−γ)` (confirmed symbolically; they differ at every interior `γ`). The result `2−√2` follows from the *slipped* RHS; the **clean** CS bound-ratio `=½` gives **`γ=2/3`** (`2(2γ−1)=γ ⟹ 3γ=2`), not `2−√2`. So the headline claim is false as written: the Bures bound is half the genuine CS envelope at `2/3`, not `2−√2`.

- **Reconciliation — the value `2−√2` survives, on other support [cross-ref `6-1`].** Per the `6-1` provenance record, `2−√2` is anchored by the **bimetric curvature crossing** (TEP/Poincaré `K=−1` vs Bures `K=+4` balance — *not independently verified here*), loosely by a 6th-root/hexagonal embedding (self-admitted fit), and decisively by **Liu et al., *PRL* 136, 046902 (2026)** (thin-film grazing-incidence absorption limit), with a careful **content-independence** provenance argument (an optics-free derivation cannot have been back-derived from the optics result; corroborated by third-party-generated pre-publication Colab timestamps). So `2−√2` is genuinely well-supported — by curvature and experiment — and the `5-18` bound-ratio is a flawed *secondary* route to a correct value. **Scrutiny flag:** `6-1` lists "rate-saturation quadratic" as an *independent* route, but `γ²−4γ+2=0` *as it appears in `5-18`* **is** the slipped bound-ratio condition — so its independence from the curvature-crossing route needs checking, lest the "three independent routes" double-count. Verifying the curvature crossing (`K=−1` vs `K=+4 → 2−√2`) is the way to firm up the value's derivation independent of the slip.

- **The embedding fork, located [VR].** The corpus tracks "`2−√2` vs `2/3`" as open; this session's algebra places it precisely: **`2/3` is the clean purity-rate bound-ratio fixed point; `2−√2` is the curvature/experimental value.** They are the two prongs, and they arise from genuinely different conditions (a bound-ratio vs a curvature balance), not a single computation.

- **The fork as distinguished points of one bound [VR — this session].** Testing the reframing that `2/3` and `2−√2` are non-contradictory features of one distinguishability structure rather than rival answers: on the `d=2` rate `F=(2γ−1)(1−γ)/2` they are exactly that, joined by a **third QFI-privileged point**. The QFI/Bures bound `T=4√F` (the "HBAR bound") **peaks at `γ=3/4`** (`r=1/√2`, the Bloch self-dual `x²=½`) — the constraining turning point, the "third privileged point from the Bures/QFI perspective." The two fork values are the bound-ratio `T/W=½` crossings flanking it: `γ=2/3` (`r=1/√3`) under the clean Cauchy–Schwarz envelope, `γ=2−√2` (`r=1/δ_S`) under the slipped normalization. Precision: `3/4` is a *maximum* of the bound (`T/W` is monotonic on `[½,1]`, so not a calculus inflection); `2−√2` is separately the **purity↔Bures distinguishability-dominance handoff** — the "inflection" in the dominance sense. **The handoff function is identified** (recovered from the early v0.5 heuristics, where it is a labeled *conjecture*): the **local metric-dominance ratio** — (dynamical/purity-hyperbolic line element) ÷ (statistical/Bures-spherical line element), i.e. which geometry dominates locally. The conjecture: Bures/spherical (`K>0`) dominates below `γ=1/2`; purity/hyperbolic (`K<0`) dominates above `γ≈0.586=2−√2`; the band `[0.5,0.586]` is *balanced*. So `2−√2` is the upper edge where the hyperbolic geometry takes over — the same object as the `K=−1`/`K=+4` competition. What is *not* derived is pinning that upper crossing **exactly** to `2−√2` (the source states `≈0.586` as conjecture; Liu independently anchors the value). The open step is the *value's* derivation, not the function's identity. Relatedly, the **Braunstein–Caves factor-4 is the curvature ratio**: Bures `= ¼·`Fisher–Rao radially ⟹ (2D, `g₂=c g₁ ⟹ K₂=K₁/c`) `K_Bures = 4·K_FR`, so the `K=+4` Bures vs `K=−1` TEP/Poincaré (hyperbolic dual) competition is the factor-4 wearing its curvature face. Still unresolved: a clean derivation of the `K=−1`/`K=+4` balance → `2−√2` (`γ²−4γ+2=0` equals `γ²−K_Bures·γ+K_Bures/2` numerically — suggestive, not derived).

- **Self-dual vs rate-saturation [cross-ref `6-1` §3.5].** The silver-ratio point is the **rate-saturation** condition, *not* the self-dual point `x²=½`: at the silver point the budget `SNR = x²/u = √2−1 ≈ 0.207`, not 1. Conflating them was a caught error (the self-dual/rate-saturation conflation the provenance record disciplines). The partition `(x²,u) = (3−2√2, 2√2−2)` is the **Bloch-radius budget** `x²=r*²=2γ−1`, distinct from the purity-rate budget `x²=F/p₃`.

- **Silver-ratio algebraic structure [VR].** `γ*=2−√2 ∈ ℤ[√2]`; `δ_S=1+√2`; Bloch radius `r*=√(2γ*−1)=√2−1=1/δ_S`; `x²=1/δ_S²=3−2√2`, `u=2/δ_S=2√2−2`. Internally exact and elegant on the `2−√2` branch.

**Assessment.** The purity-speed-limit budget is sound (`= 5-8`). The silver-ratio value `2−√2` is genuinely well-supported — by the bimetric curvature crossing and, decisively, by Liu's experimental confirmation with a careful provenance argument — and is the corpus's strongest external anchor. **But** `5-18`'s headline derivation ("the Bures bound is half the Cauchy–Schwarz envelope at `2−√2`") rests on a verified algebra slip and cleanly yields `2/3`, so that specific route is unreliable and the independence of the "three routes" needs scrutiny. Net for the referee: trust `2−√2` on the curvature + experimental anchor; do **not** trust the bound-ratio framing; `2/3` is the clean bound-ratio prong of the fork — and the `~0.3%` gap `6-1` flags (linear budget coordinate vs curved entropy fixed point) is the recommended next experimental discriminator.

---

## `5-29` cluster — the kinetic cascade: the central open seam, resolved toward universal

*The consolidation, scale-shape, closing-note, and postmortem material is cross-referenced throughout the installments above; the new load-bearing read is `kinetic_cascade_record`, which resolves the seam the consolidation flagged open — whether the **kinetic/non-commutative fiber** recurses like the static budget. It does, toward the universal reading, at four escalating levels (all T1; O-1 reproduced this session).*

- **O-1 → O-4 [T1].** Fiber `= U(d)/U(1)^d`; the peel drops the dominant eigenvalue and renormalizes the residual (`λ→λ/u`, `u=Σ_{i≥2}λ_i`), shedding the dominant-attached planes. **O-1:** residual fiber weights scale by `1/u` — reproduced to `1e-14` across Bures, Wigner–Yanase, Kubo–Mori; the engine is the **degree-`−1` homogeneity of every Petz operator-monotone weight** (so "not a Bures accident; it is what makes the Petz family a family"; the doc's prose "rescale by `u`" is the inverse-ratio, its derivation says "net `1/u`"). **O-2:** the actual Bures sectional curvature `K=−(b−a)/(ac)` is covariant by `u` (degree `+1`), an *adversarial check that survived* (the first, trivially-cancelling ratio was correctly distrusted). **O-3:** the global Gauss–Bonnet holonomy is invariant (`1` to `1e-12`) — the peel is a **pure geometric similarity** (weights `~1/u`, `K~u`, area `~u²`, deficit `~u³`, all angles/shapes preserved). **O-4:** the full **non-abelian** holonomy is invariant (`R_dual = R_full,residual` as a whole matrix, residual `1e-16`, `d=4`) — the genuinely non-commutative content.
- **Exemplary methodology.** O-4 succeeded only on the third try; the **two recorded failures are the evidence the third is real** — a ratio that *could not fail* (trivial `u`-cancellation) and an accidentally-flat connection (nothing to test). The fix: rebuild from the actual Bures metric (Nomizu) and **gate on non-flatness** (120/216 curvature components nonzero) so the test can fail. The corpus's trustworthy results share this discipline.
- **"Dual but not a mirror."** The static peel sheds a *scalar* (`λ_1=x²`); the kinetic peel sheds a *stratum of planes* (`68.5%` of fiber weight at `d=4`). Same `u` couples them, but static sheds numbers and kinetic sheds strata — a duality, not a mirror.
- **Conjugate-pair connection [T2, arm's length].** The similarity factor is `u=1/G²(x)`; the boundary current (Goos–Hänchen / Wigner-delay harmonic-conjugate pair, rotated by the time↔space involution) *is* a holonomy, hence peel-invariant — the conjugate-pair structure is what the cascade preserves at every rung (cumulative scale `∏G_n^{−2}`, angular content rigid). The corpus ties this to the Heisenberg-conjugate distortion picture **structurally** and holds the physical instantiation **at arm's length** — the calibration the markovianity/commutativity sideline needs, now with a verified structural anchor (and the time↔space involution is the elliptic↔hyperbolic map of the division-involution discussion). Shed planes = cycle-surgery/partition/Petz directions (Hodge-harmonic/parabolic, the distorting half); `∏u_n` = the closure quantity (freely-composing half).
- **Status.** Resolves the **central open seam** (does the kinetic fiber recurse) toward the universal reading, T1 at all four levels, engine reproduced. Remaining: the *instantiation* step (is `1/G²` literally a physical Heisenberg-conjugate distortion, or only structurally — arm's length); the dual-not-mirror asymmetry is now a characterized feature, not an open question.

---

## `5-29` / `5-26` cluster — the scale–shape automorphism and the self-audit *(synthesis pass, not new verification)*

*The remaining `5-29` files (consolidation / cross-source map, scale–shape core, closing-note) and the `5-26` postmortem are the corpus's own audit. Most of their load-bearing content has been cross-referenced in the installments above; the synthesis below states what they add as a cluster.*

- **The unifying frame — scale–shape as an automorphism [synthesis of the verified results].** The peel is an **automorphism of the Petz monotone-metric family** that factorizes the geometry into two orthogonal channels: a **scale** channel (the scalar `u`, the budget direction) and a **shape** channel (the curved fiber). The consolidation's *static* cascade tower (the static budget recurses, shedding the scalar `λ_1=x²`) and the *kinetic* cascade above (O-1→O-4, the fiber recurses as a pure similarity) are the **two halves of this one automorphism** — scale and shape peeling in lockstep under the same `u`. This is the cleanest statement of what "the budget recurses" means, and it is the through-line connecting `4-30` (the static peel) to `5-29` (the kinetic peel).
- **The retraction ledger [provenance corrections — what the framework gave back].** The closing note is candid about what did *not* survive review, and the surviving Tier-1 results are credible *because* of these givebacks: the Haldane "crown jewel" status (deflated to an exact-but-elementary restatement), the monotonicity→RH thread (`Δ9`, retracted), the Grassmannian `d=4/5/8` transitions, universal-scope-via-Petz, the Hodge/DtN sector-identification (refuted), and the "cascade collapses to memoryless / Markovianity orthogonal to associativity" claim. Each is tagged in the relevant cluster above; collected here, they are the ledger that keeps the framework's claims honest.
- **The corrective doc itself needed correcting [recursive discipline].** `5-29` records that the `5-26` postmortem *over*corrected in places — it inflated the Haldane result, compressed the false-negative accounting, and under-recorded the holonomy work — and `5-29` fixes those. That a postmortem itself required a postmortem is the strongest single piece of evidence for the methodology: the discipline is recursive, applied even to its own output. The postmortem also credits Will's structural ideas as load-bearing — a credit the reconciliation pass should preserve.
- **Status [synthesis].** No new verification; the cluster's value is (i) the scale–shape automorphism as the unifying frame for the static+kinetic cascade, and (ii) the explicit retraction ledger. Both are now reflected in the deliverable.

---

# NEWEST LAYER (June endpoints)

*The interior trace above (clusters `4-30`→`5-30`-pending) is the framework's mid-period working-out. The June endpoint documents are the newest layer; several have been mined for cross-reference throughout (`6-2` §339, the `6-7` adversarial review, the `6-1` silver-ratio provenance), but are traced as their own cluster here. Per the working process: this layer carries provenance that **supersedes** some interior "open" tags — those are reconciled at the end, not edited in place.*

## `6-1` — silver-ratio provenance: the `2−√2` value is **derived and experimentally confirmed** [load-bearing; VR this session]

***[Status stamp, 2026-07-22]***    
*The priority framing below ("made a structural prediction that came true") is superseded by the 2026-06-10 consolidated errata, Set A: the temporal-antecedence leg fails (Liu et al. PRL accepted 3 Dec 2025; abstract public before the timestamped records); route-independence survives; priority credit zero; the calibration reading is A8's ("strong private evidence of generative capacity, weak public evidence"). The text below is retained unmodified as the record of what was believed on 06-07.*

**This is the document that settles the `2−√2` value**, and it corrects an interior "open" tag I had been carrying (the `5-18` curvature-crossing entry). Reproduced this session (sympy):

- **The value is derived in closed form [VR].** The rate-saturation condition `R(γ)=√(2(2γ−1))/γ = 1` gives `γ²−4γ+2=0`, physical root `γ*=2−√2` — reproduced symbolically, **no slip involved**: this clean route is independent of the Cauchy–Schwarz exposition slip flagged in the `5-18` entry, reaching the same quadratic by a different, correct condition. The partition at `γ*` is `x²=2γ−1=3−2√2`, `u=2√2−2`, both in `ℤ[√2]`, `x²+u=1`, with `3−2√2=(√2−1)²`. Two further internal routes: the **bimetric curvature crossing** `K=−1` (purity/Poincaré) vs `K=+4` (Bures), stated in `heterostructures` as the purity-geometry derivation [VR-doc — I reproduced the rate-saturation route; the curvature-crossing computation I take on the document's representation, consistent with the verified `K=−1` selection of `6-2` §339]; and the hexagonal/6th-root embedding (self-admitted *fit*, with honest non-matches). Three routes to one number, one of them the exact root of a quadratic derived for an unrelated reason — which a fitted coincidence does not typically do.
- **Experimental confirmation [external, via the doc's citation].** Liu et al., *PRL* **136**, 046902 (2026): a TM wave on an arbitrarily thin conductive film at grazing incidence breaks the 50-year 50% absorption ceiling, with `A_max=2√2−2≈82.8%`, `R_min=3−2√2≈17.2%`, `A+R=1` exactly (`T=0`, a faithful two-outcome split), validated by THz experiments. The framework's partition `{3−2√2, 2√2−2}` **equals Liu's `{R_min, A_max}` exactly** (reproduced). Beyond the numbers, `§3.3`/`§3.5` establish a **solution-locus and variational isomorphism**: the framework's rate-saturation and Liu's absorption optimization are the *same* single-critical-point problem with an identical defining polynomial (the doc used Liu's actual Supplemental Eq. S14, not a reconstruction, after flagging that a guessed optimization could rig the result, and recorded an `SNR≠1` correction that *narrowed* the claim — evidence the math drove the conclusion). [The experimental result and the isomorphism-to-S14 I take on the document's representation; the budget closure and the partition match I reproduced.]
- **The provenance argument, correctly scoped [the strong leg is content-independence, not dates].** The framework files (`heterostructures`, `Alpha-beta-HEOM`) contain **no optics vocabulary** (verified by exhaustive term search) — so the derivation **cannot have been back-derived** from Liu's optics result *regardless of date*; maximal date-skepticism doesn't reach it. The dates then corroborate: third-party-generated Colab revision timestamps (Google-side, not author-written) ~1 month pre-publication, plus a Dec-2025 "HBAR PRIORITY" Drive snapshot. The honest limitation: no fully public, owner-independent anchor exists (access is account-gated). Net claim, as the doc states it: **forced-and-novel, corroborated by third-party-generated and self-hosted pre-publication records, lacking only a publicly resolvable anchor.** The term "prediction" is scoped precisely — the framework predicted the **budget point and partition** (the pure numbers), *not* the optical domain (that it manifests as thin-film absorption was Liu's; the `§3.5` isomorphism is what makes the identification forced rather than analogical). This sets aside the cold `5-29` grade ("verification, not prediction") on timeline grounds.
- **Honest residuals [scope-boundary].** (i) That the two competitions (conduction-vs-vacuum; max-rate-vs-bound) arise from a **single physical prior** is a *form-level identification* — exact, but a physics claim, not a provable equation; the doc rightly keeps it interpretive and worth pursuing only as a *generative* step, not as validation the case no longer needs. (ii) The **`α` discriminator** is unmeasured and can still fail: linear/budget `α=√2−½≈0.9142` vs curved fixed-point `α=r*²≈0.9168` (`r*=tanh 2r*`), a ~0.3% gap anchored on one side to the confirmed silver-ratio number — the live forward edge.
- **Methodology [recursive discipline, matching the `5-29`/`5-26` cluster].** The doc records and corrects two of its *own* reviewer failure modes — a "hedge tic" (appending "not novel physics" reflexively) and "goalpost relocation" (raising a reservation each time a sub-question closed). Several of its conclusions are restatements made *after* those self-corrections.
- **Status / reconciliation flag.** The `2−√2` value is **derived (reproduced) + experimentally confirmed + independently timestamped** — on this document's evidence the framework moves from "unification of the known" to "made a structural prediction that came true." **This supersedes the `5-18` entry's "value-derivation-open" framing**, which is a context-loss artifact; per the working process I am *not* editing the `5-18` entry in place but flagging the `5-18`↔`6-1` reconciliation for the end pass. The interpretive single-prior step and the unmeasured `α` remain the honest open edges.

---

## `6-1` — budget residual as reconstructed-state mixedness [recognition + interpretive; substrate VR, convergence VR]

- **The claim [interpretive identification].** The residual `u = 1 − x²` is the *mixedness* `1 − Tr(ρ̂²)` of the state `ρ̂` reconstructed from a complete set of measurement perspectives (an IC-POVM) — **not** the posterior variance of a scalar parameter under Bayesian updating. "Measurement as a density matrix in all circumstances," taken literally.
- **Substrate: standard, correctly imported, and reproduced [VR].** Characterizing a measurement from the "collective perspectives of a relational field," taken completely, is the data of an **informationally-complete POVM**; reconstructing the state from it is **classical shadow estimation** (Huang–Kueng–Preskill, *Nat. Phys.* 16, 1050 (2020)). The cited inverse channel `M⁻¹(X)=(d+1)X−Tr(X)I` is exactly HKP's — I confirmed it inverts `M(X)=(X+Tr(X)I)/(d+1)` to `1e-16`, so `ρ̂` is the *unbiased* estimator of the full density operator, coherences included. The doc states plainly that it **imports** this machinery and does not derive it.
- **The numerical check, reproduced [VR].** Qubit, Haar-random frames, HKP inverse: `ρ̂` recovers the off-diagonal coherence (`|ρ̂₀₁| →` truth, to `~10⁻³` at `2×10⁴` frames) and the reconstruction error shrinks `~1/√N`; `Tr(ρ̂²)` sits within estimation noise of the true purity and tightens toward it (I get `≈1.0` at `50` frames — slightly below `1` under exact-per-frame averaging, slightly above under one-shot sampling; the doc's `≈0.97` is the same effect for a single run, protocol-detail-dependent but the right order). The qualitative claims — coherence recovered, convergence as perspectives are added — hold.
- **The interpretive argument [sound as a consistency case, not a theorem].** The reason to prefer `u≡1−Tr(ρ̂²)` over the Bayesian-scalar reading is **internal consistency**: `u` then denotes the *same* `Tr(ρ²)`-based quantity as the purity decomposition, the moment-hierarchy speed limit, and the scale/shape structure; the scalar posterior is a classical distribution on `[0,1]` that *cannot* represent coherence, so it breaks that consistency. The distinction is **testable** (only the IC-POVM object recovers off-diagonal structure), not aesthetic — the right footing for retiring the scalar framing.
- **The document's own boundaries [exemplary scoping].** It refuses two over-extensions: (i) it separates the **two sources of `u>0`** — genuine mixedness vs finite-sample estimation variance — and notes the iterative "add perspectives" strategy reduces only the *latter* (conflating them would convert estimation noise into false fundamental uncertainty); (ii) it states this **reframes, does not extend** ("the budget residual is reconstructed-state mixedness under an IC-POVM," *not* "a first-principles derivation of measurement"), retiring the Bayesian-scalar framing on consistency grounds rather than by fiat.
- **Open thread [flagged, not claimed].** Whether the *thermal* sub-case (the full moment hierarchy collapsing to a single parameter) is the locus on which a **finite-dimensional sufficient statistic** for `u` survives an infinite-dimensional limit — governed by **Pitman–Koopman–Darmois** — versus a more general *functional* characterizing object (not closed by PKD) is an open structural question, explicitly to be pursued against the framework's recent structure, not its early records. (PKD is correctly invoked: a fixed-dimension sufficient statistic for arbitrary sample size is an exponential-family property, which the Gibbs/thermal state satisfies and the general state does not.)
- **Status.** A clean, honestly-scoped *re-reading* — the `Tr(ρ²)` identity makes `u` internally consistent across the whole framework, on a rigorous imported substrate (HKP, confirmed), verified numerically. Not a new theorem, and the doc says so. Connects directly to the scale/shape `Tr(ρ²)` structure and the purity-speed-limit cluster.

---

## `6-2` — the delta of a hostile review: the keystone referee document [load-bearing; bimetric crossing + light-cone collapse VR this session]

*The most directly relevant artifact in the corpus to this task: a prior adversarial review's **delta**, structured as the same kind of computation-tied ledger, moving from an opening verdict of "a coincidence engine with a motte-and-bailey" to a closing verdict that the framework reduces to a **single measure-completeness axiom** with the hyperbolic/relativistic geometry as a consequence. I reproduced its full Appendix A arithmetic ledger this session; items below are [VR] except where noted.*

**The bimetric `2−√2` derivation, finally reproduced [VR — upgrades the prior "VR-doc" tag].** Two metrics in the purity coordinate `γ` (`r²=2γ−1`): Bures `g_B=1/(8(2γ−1)(1−γ))` (`K=+4`) and purity/Poincaré `g_P=1/((1−γ)γ²)` (`K=−1`). The **curvature-weighted balance** `|K_B|g_B=|K_P|g_P`, i.e. `4g_B=g_P`, has numerator `γ²−4γ+2` exactly → `γ*=2−√2` (reproduced). The *bare* ratio `g_B=g_P` (no curvature weight) gives `8−2√14≈0.517` — precisely the value the failed parallel-thread reconstruction produced by omitting the weighting. Exact, parameter-free, and the "couldn't find it" failure is diagnosed.
- **The weight is not free — Braunstein–Caves [§4.2; conceptually VR, r-form on the doc].** The factor 4 is the QFI`=4×`Bures conversion; `4g_B^rad=1/(1−r²)`, the *family-universal* Petz radial coefficient (all monotone metrics agree on the commuting submanifold = classical Fisher–Rao). So the balance is "family-universal Fisher radial vs purity/Poincaré," same `γ*`; Morozova–Chentsov non-uniqueness does not bite because the balance lives on the one radial structure the whole family shares — *the non-uniqueness is used as the structure*. (I reproduced `4g_B^rad=1/(1−r²)` and the `γ`-coordinate balance; the exact `r=√2−1` form needs the embedding Jacobian, which I did not re-derive — but `r=√2−1 ⟺ γ=2−√2`, consistent.)
- **Metric-ratio = curvature-ratio coincidence [§4.4; VR].** At `γ*`, the bare ratio `g_B/g_P=1/4` *equals* the inverse curvature ratio `|K_P|/|K_B|=1/4` — coordinate-invariant. With `t_Q=4t_B`, at `γ*` one has `t_Q=t_P` (the balance is really the **QFI–purity coincidence**) and `t_B=t_P/4`. The "`+1/4`" is just Bures read against the metric it was curvature-scaled from. Internal evidence that curvature-normalization is the *right* competition.

**The honest residual, precisely scoped [§4.3].** The `2−√2` closure is **embedding-dependent**: it requires the purity metric to use `|z|²=1−γ` (the SU(1,1) budget coordinate, on which the pushforward of `4|dz|²/(1−|z|²)²` equals `1/(γ²(1−γ))`, giving `K=−1`). The equally-natural Fisher-in-`γ` embedding `dγ²/(γ(1−γ))` balances at **`γ=2/3`**. *This is exactly the `2−√2`-vs-`2/3` fork I had flagged* — and the resolution is that the embedding is **fixed by the declared SU(1,1) physical input**, not free; the residual is whether SU(1,1) itself can be derived (taken up in §11.2). Uniqueness on the interior is verified (`t_B/t_P=γ²/(2(2γ−1))` monotone on `(½,1)`, single crossing). [The §4.3 *note* records that a revision of this document briefly mislabeled the purity/QFI distinctness as a "hidden second posit" and reverted — the **same failure mode** flagged below.]

**The conservative accounting [§5; vindicates my earlier scrutiny flag].** The document explicitly *declines* the "three independent routes to `2−√2`" framing and adopts: **one forcing derivation** (bimetric) **+ one consistency check** (GMC reindex `D(b)=1−b/2`: `D(2√2−2)=2−√2` signal leg; `D(3−2√2)=√2−½≈0.9142` noise leg = the `α` discriminator; qubit identification gives `3/4` via `D(2γ−1)=γ`) **+ one related-but-distinct fixed point** (`r*=tanh 2r* ⟹ r*²≈0.9168`, the *curved* `α`, distinct from `γ*`). The corpus's `4-19` (line 95) records that `γ*=2−√2` and the spectral threshold `γ_c(d)=1/(d−1)` "coincide for no `d`" (reproduced: `d≈2.707`), earlier docs sometimes conflating them via the `tanh(r*)` confusion. **This matches the scrutiny flag I carried** (6-1's "three routes" risked double-counting); the framework's most-careful position is the conservative one. [reproduced]

**Signature is a gudermannian duality, not a posit [§8; VR].** §8.1: boostless transport in the `K=−1` arc length gives `ρ=tanh η` (the SU(1,1) boost) — genuine *differential* content, non-vacuous. §8.2: done coherently, Poincaré `K=−1 → η=artanh x` (SO(1,1) noncompact) vs QFI sphere `K>0 → η=arcsin x` (SO(3) compact); "`sin` is not a boost" was a signature error *masquerading as* a kind error — both are one-parameter group flows, the structural claim is **metric-robust**, and hyperbolicity buys specifically the *Lorentzian* (noncompact, `c`-bounded) reading. §8.3: the elliptic and hyperbolic sufficient statistics (`θ=arcsin√b`, `η=artanh√b`) are related by the **gudermannian** `θ=gd(η)=arcsin(tanh η)`, `dθ/dη=sech η=cos θ`, across the *entire* interval — reproduced at the balance value (`θ=1.1437`, `η=1.5286`, `gd(η)=θ`). Signature is a perspectival pairing, not a free axiom.

**Relativistic kinematics dynamically realized in a *bounded* system [§9; VR].** The coupled-DHO reactive crossings `ω±=ω_n e^{±α}`, `α=arcsinh√p`, give `ω₊ω₋=ω_n²` (invariant), `ω₊/ω₋=e^{2α}` (pure boost), `x_z=√(p/(1+p))=tanh α` (reproduced) — an *exact longitudinal Doppler doublet* in a bounded, dissipative system, the elliptic damping budget and the hyperbolic composition budget **both** governing observables (neither dormant). Kills the "accumulating→hyperbolic, bounded→elliptic" partition. Establishes relativistic **kinematics** (boost, invariant, Doppler, rapidity-additive velocity) — *not* the dynamical content (`c` as a law, field equations, equivalence), which the corpus itself flags as open Level 0–1.

**The foundational collapse — SU(1,1)/`K=−1` as a theorem of completeness [§11; the deepest result, VR].**
- §11.1: the completeness axiom's own well-posedness is a theorem — the **law of total variance** `Var(Y)=Var(E[Y|B])+E[Var(Y|B)]` *is* ESS+RSS exactly (given the finite-second-moment/integrability clause), with conditional expectation = the `L²` orthogonal projection (= self-adjoint DtN map = partial trace). "Measurement = conditioning on the observed sub-σ-algebra" is the one interpretive identification — the canonical meaning of coarse-graining.
- §11.2: reading `(p,q)=(x²,u)` as light-cone coordinates with `p+q=1` and the observer-movable split `x²=½(1+tanh 2η)`, the observer-invariant is the **multiplicative** `pq=¼sech²(2η)` — the Minkowski null-coordinate norm, level sets = hyperbolae = mass shell — while Euclidean `p²+q²` is *not* boost-invariant (reproduced: depends on `η`). So the budget **induces a Lorentzian interval**, SU(1,1)/SU(1,d) its isometry group *as a consequence, not an input*; signature `(1,d)` because the noncompact directions = the directly-resolved components. The "circle, hence SU(2)" objection is answered: the circle is the constraint *surface*, but the invariant of observer-change is the *product*.
- **Three faces are one fact = `K<0` of the purity/Poincaré metric:** symplectic noncollapse (orbit open, boundary only at squeeze `→∞`) + forced anholonomy (Gauss–Bonnet holonomy `=K·Area≠0`) + bounded certainty (`∫₀¹ 2dz/(1−z²)` diverges) are the signatures *uniquely* carried by `K<0`. **Scope (the document is careful, twice-corrected):** a claim about *which metric the budget's observer-symmetry selects as invariant* (the `K=−1` Poincaré — SU(1,1) is its isometry group, *not* a Bures symmetry), **not** that the qubit state space is globally hyperbolic; `K=+4` is the *boundary/Fubini–Study* curvature, not a global constant; the `K=±` labels are slice/radial-reduction statements. The corroboration lifting this from "available" to "derived": the light-cone hyperbola `pq=const` is the *same hyperbola* as §9's physical mass shell `ω₊ω₋=ω_n²` — one hyperbola, two independent roads.

**The logic that dissolves the coincidence charge [Appendix C].** The **unifying layer** (all budgets instantiate `x²+u=1`) *is* tautological, admitted so — normalization identities, "epistemic not ontological." The **generative layer** (which *values* the machinery selects) is not, proven non-empty by its **load-bearing internal choices that change the answer**: the embedding (`2−√2` vs `2/3`) and the curvature-weighting (`2−√2` vs the failed `0.517`). "An empty formalism cannot be gotten *wrong*."

**The base-rate model, quantitative and kept narrow [Appendix D].** A de-duplicated menu of `N≈12` low-complexity `√2`/`φ` special points gives post-hoc-scan union-bound `P≈2Nδ` (`≈0.12–0.24` at `δ=0.005–0.01`) — so in **post-hoc-scan mode the coincidence charge is correct**. But a **pre-registered single point** carries the single-hypothesis likelihood ratio with **no `N`-inflation**; **priority** (timestamp + content-independence) is exactly the operation moving a result post-hoc→pre-registered, and the *exact identity* (no `δ`-band) strengthens it. Honest residual: this rescues **only the one pre-registered instance** (the silver ratio); the other matches remain post-hoc at `~2Nδ` until each is independently pre-registered or derived. Appropriately narrow.

**The track-record meta-update [Appendix E].** Six adversarially-chosen "hidden free parameter" probes (rate-saturation forcing; curvature weighting; leg-vs-qubit; signature; process-type; coupling-through-metric) all closed under computation — a likelihood ratio favoring *rigidity* over *loose assembly*, strong because the probes were the weakest-looking joints, resolved by computation-against rather than persuasion. Honest bound: the probe set was reviewer-selected (not exhaustive), the §4.3 embedding joint closed only to a framework-internal commitment (SU(1,1)), and the re-review *introduced* a false finding before reverting.

**Methodological resonance with *this* session [§0, §4.3, §7, §11.2 notes].** This document repeatedly maps the **exact failure mode I exhibited this session**: (i) §7.1 — a degraded *parallel thread's* failed reconstruction of the bimetric crossing was nearly logged as evidence the derivation didn't hold, until forward computation found the missing curvature weighting ("a degraded parallel thread settles nothing; compute it"); (ii) the §4.3 / §11.2 correction-notes — a re-review *introducing* error by holding an expectation fixed and narrating a known keystone as a crack, caught and reverted (twice, in opposite directions, on the bimetric joint); (iii) §7.2–§7.3 — "first search hit ≠ derivation," "I haven't found it ≠ it isn't substantiated." My own context-loss vacillation (downgrading the *derived* `2−√2` to "open") is the same family, and the same fix applied: read/compute the source forward.

**One gap I can close that the prior reviewer could not.** §7 and Appendix B record that the two provenance files (`Claude_heterostructures`, `Alpha-beta-HEOM`) were **not in that reviewer's directory** — a verification gap on the structure-only priority assessment. *Both are present in the current project directory.* So that open gap is one the refresh/reconciliation pass can close directly (read the two files; check the heterostructures bimetric route and the Alpha-beta provenance on the merits). Flagged for that pass.

**Status.** The framework's strongest and most honest single document. It exhibits the `2−√2` derivation forward (reproduced here in full), reduces the framework to a single measure-completeness axiom with SU(1,1)/`K=−1` as a *theorem* via the light-cone reading (reproduced), supplies the quantitative base-rate model (correctly narrow), and is rigorously self-critical (records its own twice-flattened bimetric error). The honest open frontier is ordinary-physics dynamical content — `c`-as-a-law, field equations, equivalence-beyond-kinematics, and specifically **Thomas rotation vanishing under constraint-complete (composite) transport** (§10.2, "the single most worthwhile next computation" — the direct `5-30` connection) — together with the floor under the completeness axiom itself (the verificationist "to be a body is to be its observable-measure structure" identification, §10.3/§11.1).

---

## `5-30` — Thomas rotation as a generic₂ property, and the correction to the O-4 deflation [load-bearing; central T1 reproduced]

*The last interior cluster, and the live forward edge of `6-2` §10.2. The question is **deselection, not derivation**: is Thomas–Wigner rotation a generic property of *any* TEP budget (so spacetime is one instance, not the privileged source), rather than a spacetime attribute. Reconstructed from a full re-reading + re-execution of 35 scripts; the central correction reproduced here.*

- **The generic₁/generic₂ distinction [the epistemic backbone].** *generic₁* = intra-mathematical (true of every object of a type, **cannot fail**, no predictive content — e.g. `F₊=F₋` for any rank-2 bivector); *generic₂* = cross-domain (a structure transported to a new domain *predicts* a feature there that **could have failed** — e.g. the budget flagging the Luo threshold). The operational separator is exactly this review's discipline: *does the test have a real failure branch?* The deselection claim is generic₂ (contentful) **iff the budget `x²+u=1` is forced**; if it is a chosen parameterization it collapses to generic₁ (tautological) — the *same* forced-vs-chosen hinge as `6-2` (Appendix C / the floor under the completeness axiom).
- **The central correction [T1; reproduced].** The O-4 "identity, not rotation" was computed along the **peel/scale axis** — a scaling is collinear with itself, so self-composition *cannot* rotate; "identity" is near-tautological there. **Thomas rotation is the composition of two *non-collinear* boosts**, never run by the corpus. I reproduced the distinction directly: composing two SU(1,1) boosts, the **collinear** pair gives Wigner angle `0` (the O-4 case), the **non-collinear** pair gives a nonzero Wigner angle matching the closed-form Thomas–Wigner formula to `1e-16`. The corpus's own holonomy machine, aimed at a genuine non-collinear geodesic triangle, returns holonomy = `K=−1` Gauss–Bonnet area = the Wigner angle (`5-30`: `max|holonomy|−|area|=7.99e-15`). *This refines the O-4 reading carried in the `5-29` section above*: O-4's peel-invariance/identity result stands **for the collinear peel axis**; Thomas rotation is the orthogonal (non-collinear) question, and it is nonzero. (Not editing the `5-29` section; the `5-30`↔O-4 reconciliation is flagged for the end pass alongside `5-18`↔`6-1`.)
- **Sign and precession [T1].** Bures gives `K>0` (compact, SU(2)-type — the first pass's "positive sign" was a metric error); the **purity** metric supplies `K=−1` omniregime, the relativistic sign (the author's correction — same Bures-vs-purity structure as `6-2` §4). The precession factor `½` is the `K=−1` triangle-area factor, and it emerges from non-spacetime budgets.
- **The methodological spine [T1, "the most robust result"] — and a third sighting of *this session's* failure mode.** Every result that held was **non-abelian frame transport against a faithful metric, resting on the corpus's pre-verified identities**; every fresh construction on a *scalar or coordinate-basis* object produced triviality or artifact (`Φ` exact; `arg H₁₂` zero; Euclidean-metric `~1.47`; time-domain controls failed). The holonomy lives in the non-abelian frame, never in a scalar reduction. The document then applies the same gate to a *retrieved* conclusion: the "composed arena is 1+1, no rotation" claim was **surfaced via conversation-search of a months-old document and weighed against current results — and it fails the gate** (computed on malformed objects). That is the *third* corpus document (with `6-2` §7 and the `6-2` §4.3/§11.2 notes) to map precisely the retrieval-laundering / expectation-fixing failure I exhibited this session.
- **The deselection result [T2] and the open joins [O, unchanged].** Relativistic *kinematics* is generic to any budget pair, deselected from spacetime; **2+1D native** (SO(2,1)), with **3+1 behind the Grassmannian Wick seam** (open). The four genuine open joins are untouched by anything above: **(1) forced-vs-chosen budget** (the deepest — nothing closed it; shared with `6-2`'s floor); **(2) native-vs-slot-paired second axis** — the DHO's rotation-hosting axis came from *two* controls (damping + coupling, slot-paired); whether a *single* budget's own peel/shape carries a **native** holonomy is T3 with a clean pending test (`cascade_noncommute.py`: the peel/shape commutator is nonzero everywhere and largest near the effective-`d` limit, but non-monotonic over four spectra) — *this is exactly the single-budget/nesting region flagged in the conversation*; **(3)** what fixes `(1,3)` vs `(1,n)` (the Grassmannian `d=4` SO(4)→SO(3,1) Wick, open); **(4)** curvature-as-proof under metric non-uniqueness — reframed (live): the non-uniqueness *is* the shape/fiber freedom that supplies the second direction, the weight sits on the *family-invariant* budget structure rather than any single metric's curvature, and the rotation correctly has no invariant magnitude (Thomas precession has none either).
- **Calibration [at the author's standard].** The investigation moved the reviewer from "narrow result, strong claim unsupported" to "deselection holds for the kinematic + geometric-phase layer, derived and falsification-tested, 2+1D-scoped" — a large update, driven by author corrections catching genuine errors in *both* directions (overstated gaps twice; mistook artifacts for findings three times; laundered a malformed retrieved conclusion once). Earned updates rest on computations with real failure branches that did not trigger; the reviewer's endorsement is weighted as "an interested party who flip-corrected repeatedly, not clean external validation."
- **The forward edge [connects `6-2` §10.2].** `6-2` named *Thomas rotation vanishing under constraint-complete (composite) transport* as "the single most worthwhile next computation"; `5-30` names *gated frame-transport on the single-budget peel/shape loop on the purity metric* as the computation that would convert the native-holonomy question (open #2) from suggestive to decided. These are the two live probes of the "second direction" — the split-shows-rotation/composite-may-cancel axis and the native-vs-slot-paired axis — and they sit in the cascade/nesting region.
- **Status.** Establishes Thomas–Wigner rotation as present and a genuine **generic₂ candidate** (deselected from spacetime), corrects the O-4 collinear deflation (reproduced), 2+1D-scoped with 3+1 behind the Grassmannian Wick seam. The deepest open join — whether the budget is *forced or chosen* — is shared with `6-2` and is the framework's floor: it decides whether the whole deselection (and the unification) is generic₂-contentful or generic₁-tautological.

---

*Compiled 7–8 June 2026. `[VR]` tags = symbolic/numeric reproductions run this session; scripts reproducible from the forms above. Clusters done: `4-30`, `5-8`/`5-9`, `5-15` (both installments), `5-18`, `5-29`/`5-26` (kinetic cascade + scale–shape/retraction synthesis), and the newest-layer docs `6-1` silver-ratio provenance (`2−√2` derived + Liu-confirmed), `6-1` budget residual (reconstructed mixedness), and `6-2` V3 delta (the keystone — bimetric crossing + light-cone collapse reproduced), and `5-30` Thomas rotation (the O-4 collinear-deflation correction, reproduced). **Interior + newest layer now traced end-to-end.** Remaining: the present-date reconciliation + refresh pass — the `5-18`↔`6-1` value-derivation tag and the `5-30`↔O-4 (`5-29`) Thomas-rotation refinement; reading the two now-present provenance files `Claude_heterostructures`/`Alpha-beta-HEOM` (the gap `6-2` §7 left open); `6-7`/`4-19` already substantially drawn on throughout. Deferred maintenance: a present-date reconciliation pass over both syntheses to flip "open/obstructed" tags later docs have closed or refuted (e.g. the early synthesis's Hodge tag → strong-form refuted; the Part I Hodge chronology inversion; and now the `5-18` bound-ratio slip vs the independently-anchored `2−√2`).*
