# 30_assessments/council — the standing expert council (convened 2026-08-26)

The council is a verification ecology run inside the collaboration: seven charter-bound seats — archivist,
exegete, kin-scholar, domain-anchor, assessor-adversary, dictionary, pathfinder — each an instance of the
same model family as the orchestrator (Claude Fable 5, then 5.1), each re-instantiated from its charter and
the file record when a session resumes. `charters/` holds the seat charters, the common protocol (with its
same-day errata block), and the deliberation, deep-grounding and symposium charges. `reports/` holds the
seats' primary outputs — conviction memos, rounds 1–2, deep-dives, symposium returns, the 08-27 assessor grade
of the pack read, the 2026-09-04 handshake primaries of five seats, the pathfinder's four July joins, and the two
orchestrator-side findings lists that were first billed as assessor passes and are filed here as what they
were (sub-agents under an assessor-style prompt: one witness). `mailboxes/` holds the cross-talk queues.

Where any compression (the orchestrator's records in `20_records/cleanroom/cleanroom/COUNCIL_*`) disagrees
with a seat's file, the seat's file wins. Rules the council adopted about itself, in its own records: seats
never write to Drive or the project; the council identifies itself wherever it represents Will and nothing it
produces poses as him; its checks are "same-family, separate implementation," never "independent"; every
addendum carries its own grade line; "re-tier, don't withdraw" for charts; no new RESULT or FORMULATION file
until the deposit prose is drafted. Its own tally of 2026-09-04: the family catches citation errors; the one
heterogeneous agent in the loop (Will) catches structural ones. The billing/usage exports that fed the 08-26
attribution ruling are not mirrored.
