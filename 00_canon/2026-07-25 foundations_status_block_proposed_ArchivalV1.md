# Proposed canon patch: the Foundations Status block    
**[ArchivalV1, 2026-07-25]**

***SUPERSEDED SAME DAY***  
*by Foundations Status block v2, appendix of correctness_patch_bundle_DraftV1.md (adds the Yamagata bracket, the 06-15 discharge, Theorem C/W1, the 2PH-member row). Cite v2 only.*

*Per Addendum 2 §3.1 — a single always-visible block for the Central Reference and repo README, so the status of the foundations can no longer be mis-cited by compression. Ten lines, three strata, pointers. Wording offered; the countersign edits.*

---

## Foundations Status (as of 2026-07-25)

**Derived [T/V], twice over — the budget.** x² + u = 1 is the partition of unity of a passive invariant ensemble (positivity constitutive; passivity ≡ boundedness ≡ positive-realness ≡ normalization one *derived* equivalence class — 06-09 §§2–3), and independently the exact signature of *any* passive state-preserving interface (Theorem P, three-line proofs, 07-19). The budget is never assumed.

**Forced output [T/V] — the metric.** Measure-completeness ⟺ statistical sufficiency ⇒ L² (law of total variance for every conditioning ⟺ conditional expectation an orthogonal projection; fails in every Lᵖ, p ≠ 2) ⇒ Riemannian ⇒ Fisher–Rao the unique quadratic germ (Čencov/AJLS) — Thread L, 06-29. The two extensions beyond the Chentsov regime are the decomposition's two axes and exhaust the independent relaxations: Campbell (normalization → scale/cone, two mass functions) and Petz (commutativity → shape/fiber, the monotone family) — 06-09 §6 [V]. The arena is pinned: ℝH²/SU(1,1) via the exclusion battery, modulo (H*) — O-a note, 06-10. **The Petz-f multiplicity is the bounded shape residual inside that pinned space — structure, not slack; there is no metric-selection question at any stratum** (06-20 [I1]; 6-02 §4.2; A7). Retired vocabulary: "metric selection," "the forced-vs-chosen floor."

**The itemized remainder [O, with routes].** O-a residue: (H*), the budget-arena relativity principle — posited or derived, derivation target named (completeness/conditioning; O-a → O-d). O-d: sufficiency structure from completeness + dilation [A→T candidate; route: Stinespring/Naimark + AJLS §5; rung 1 executed 07-25 — the two-generator factorization reduces the classical case to the (D) closure framing]. O-e: the (1,d) counting at d > 2 (instantiation-anchored). One base postulate: compositionality/scale-independence (G-multiplicativity). One applicability clause, not a gap: whether a given *physical* register instantiates the axiom — per-register and empirical (theorem in the arithmetic register via Peter–Weyl, Delta 7 §1); discriminating force sits only in specific numeric commitments (completeness-reduction, 07-19).

*One-phrase summary when needed (A7 verbatim): forced at the metric-space level, modulo O-a/O-d/O-e.*
