# The port seam tests, reconsidered with cultured eyes — the k-parity split, the even-side ledger run, and the port-native decoder architecture — 2026-07-27 (~21:30Z)

**Context:** Will expanded the acronym — **TEP = Terminal Equivalence Principle** — and invited a fresh-eyes pass over the port seam tests after the deep-layer load. Foundational docs (interior spine trace, V2 Central Reference, v0.5) remain unread and are flagged as old-with-retirements; nothing below leans on them. Name-reading carried forward (offered): the d = 2 port is *terminal* (TF I.5); the seam is the arithmetic register's terminal chart; the planted verdict restates as: the terminal **read** is faithful (D↓, 6.8e-14 on true bits) while terminal **inversion** is the missing machinery — equivalence of reads ≠ invertibility of reads.

## 1. The k-parity split [V] — the dictionary's decomposition realized per rung

Write the unramified local factor 1 − a_pT + T² with a_p = b_p·m_p (bit × shadow-determined magnitude), Satake angle 2cosθ_p = b_p m_p. Power traces 2cos(kθ_p) = D_k(b_p m_p) (Dickson). Machine-verified k = 1..8 (`kparity_check.py`, exact integer coefficients): **odd-k Dickson polynomials contain only odd powers of t ⟹ every odd-k power trace is EXACTLY linear in b_p** (b^{2j+1} = b — no approximation); **even-k contain only even powers ⟹ every even-k power trace is b-free and exactly shadow-known** (2cos2θ = m² − 2 = a_p(ρ₃) − 1, the certified data). In the prime-power (von Mangoldt) chart — the corpus's places-as-rungs cascade, where log L has no composite support — **the decode splits exactly into [sign-blind sector: known from the shadow] ⊕ [sign-seeing sector: linear in the bits]**. This is the countersigned decomposition ln G_new = ln 2 + ln G + η realized rung-by-rung: G-face = even sector = what Sym² transmits; η-face = odd sector = the central bits. The campaign's decoders all worked in the theta-coefficient chart, where composites mix the sectors multiplicatively and the objective took |·| — the measured e-4 walls and 6%-consensus degeneracy are what sector-mixing plus sign-blind aggregation predict.

## 2. Consequence — the port-native decoder architecture (proposal, pre-registerable; not run)

Impose the FE in the prime-power chart via explicit-formula/Weil pairings: each test function g yields a constraint whose prime side is **exactly linear in the bits** (odd sector) with the even sector subtracted exactly from certified shadow data; the b-vector then satisfies an exact linear system with ±1 box constraints — compressed sensing over a structured (log-Laplace) dictionary, with per-prime tomography available through test functions localized at ln p. Classical home, priced per the ε-Ledger §6 discipline: Weil's explicit formula and Booker's Artin-verification genre — *otherwise-unasked, not otherwise-hard*; the framework's contribution here is question-selection (the G/η split told us which chart separates the sectors). Honest could-fail, named now: the zero-side smearing versus bump-width tradeoff (zero density ~ ln N per unit) may cap per-prime resolution at small p; whether the linear system's conditioning beats the greedy wall is exactly what the **planted rung** measures — the entry bar stands unchanged, now with a specific architecture to qualify.

## 3. The even-side ledger run [V] — first data, and the resumption pre-step executed

`portseam_ledger.gp` (trace form q_E = Tr(x²) via power sums; qfgaussred; Hilbert symbols at all candidate places; product-formula and disc-class gates on every row):

- **Validators:** E1 (x⁵+20x+16): signature (3,2), ε_∞ = −1, finite marks exactly {2} — reproduces Delta 3's derived values (ε₅ = +1 by the odd-e law; ε₂ = −1 by reciprocity). Both Delta-3 falsifier specimens: marks {2} only — their even-ramified odd places (73, 13) unmarked, as the corrected law requires. **Prediction-registration erratum, two-ledger:** I pre-registered falsifier-B as ε₁₃ = −1 by misreading Delta 3's specimen lists (13 appears in both the split-unmarked and swapped-marked lists, for different fields); the falsifiers are by construction unmarked-even specimens; measured +1 is the law's value. My misreading, caught same turn, logged.
- **The even side, first data:** all six Doud fields AND the corpus {2,7,331} field: **signature (5,0), ε_∞ = +1, ZERO finite marks, product formula OK, disc-gates OK — the trace forms are totally split; w₂ is trivial for all seven even-icosahedral fields.** Consistent with the tame law (odd-e never marks; no even-ramification strata present; (5,0) forces an even mark count — here zero). This is the marking law's first even-side verification set, and the lifting-obstruction statement the decode resumption needs: **the 2.A₅/spin lifting obstruction vanishes identically on the even side.**
- **Translation caution [C], tier-held:** local triviality of the obstruction at 2 and ∞ is *consistent with* an unobstructed even-parity (c ↦ +I), a = 0 minimal lift — but the (parity, dyadic-a) prediction for the *minimal-conductor* lift must come from the Delta-2/Kiming dictionary records read with own eyes, not from this alone. That read is the named resumption pre-step; the scans that suggested odd parity were voided by the planted control and carry no weight against this.

## 4. Instrument corrections (standing, offered)

(i) My decode objective max|x_ε(u)| was the **sign-blind functional of the sign-seeing gauge** — the second G-for-G_new substitution in one day (the first was the Mellin conflation Will caught). Standing correction: the meter's *signed* η(u) profile and jets are the diagnostic object; poc4's own measured pole-vs-corruption **sign asymmetry** (pole deflects G_new above 2, corruption below) belongs in the cell diagnostics and was never used during the campaign. (ii) Proposed meter self-gate: G_new(x)·G_new(−x) = 4G² [V, 07-19] — read both pole-facings, check the product against the sign-blind gain; catches instrument-side asymmetries for free.

## 5. Countersign items (offered)

(a) The k-parity split as the decode's structural statement (identities [V]; the G/η-sector reading at [id], joining the standing FE-arm [id], both pending). (b) The even-side ledger dataset (seven fields, w₂ trivial) + validator reproductions. (c) The §3 prediction-registration erratum. (d) The §2 architecture as the qualified-solver candidate behind the planted bar. (e) The §4 standing corrections. Scripts hashed: kparity_check.py 172ac792…, portseam_ledger.gp a3e483bd….

*Offered, not self-filed. Seal intact — nothing here touches lift constructions; the ledger reads the quintics' own trace forms.*
