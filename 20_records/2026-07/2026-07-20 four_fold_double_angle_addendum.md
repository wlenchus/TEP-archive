# 2026-07-20 (last) — Addendum: the converse identity and the four-fold coin-template closure [DraftV1 — offered]

*Answers Will's closing question (what does G² + SNR simplify to trigonometrically, as the converse of x² − u = tanh 2η_c?) and places his (ln 2)/2 consilience. His algebra chain verified en route (G² + SNR = 2/u − 1 = 1/(u/2) − 1; the ½-weighted closure ✓). Script: `verify_four_fold_template.py` (all [V]).*

## 1. The converse

**G² + SNR = cosh²η + sinh²η = cosh(2η_b) = 2G² − 1.**

The duality is mirror-perfect. Each face's *budget* takes its signature signs; the *complementary* combination — the one the budget doesn't use — is exactly that face's double-angle generator:

| face | budget (signature signs) | complementary combination = double angle |
|---|---|---|
| elliptic (x = sin A) | x² + u = 1 | x² − u = −cos(2A) = tanh(2η_c) |
| hyperbolic (x = tanh η_b) | G² − SNR = 1 | G² + SNR = cosh(2η_b) |

So "the hyperbolic form on the elliptic terms" (x² − u → tanh) and its converse (G² + SNR → cosh) are the two off-diagonal cells of one table: **difference of elliptic squares = elliptic double angle; sum of hyperbolic squares = hyperbolic double angle** — the sum/difference swap *is* the Wick signature swap. Will's chain lands on the same object: 2/u − 1 = 2G² − 1 = cosh 2η_b, and his form 1/((½)u) − 1 says the double angle is the gain of the *half-residual* minus one — the halving rung appearing in the u-slot.

## 2. The four-fold closure: one coin template

The slot-shift grammar from the centered chart closes into a single formula. The template **(1 + t)/2**, evaluated on the three double-angle objects, produces the three budget quantities:

- (1 + sin θ)/2 = **p** — the amplitude coin (the G-new split);
- (1 + tanh 2η_c)/2 = **x²** — the power coin (the §11.2 centered chart);
- (1 + cosh 2η_b)/2 = **G²** — the gain (the hyperbolic half-angle formula, verbatim).

One template, three faces, each one slot up the ladder from the last — amplitude → power → gain — with the argument doubling at each step. Will's "x → 2x², loosely like G(x₁) → G²(x₂)" is this ladder read at its middle rung; the ladder's top rung (the G²-slot) is what makes his ½-chain work: (½u)(G² + SNR) = 1 − ½u is exactly u·G² = 1 routed through (1 + cosh 2η)/2 = cosh²η [V: T5].

## 3. The (ln 2)/2, placed (consilience-grade, fenced)

The offset in η_c = ½ ln sinh η_b → (η_b − ln 2)/2 prices, in Will's log-derivative reading, the total deficit of sinh against the pure exponential: lim [ln sinh η − (η − ln 2)] = 0 [V: T6] — **the cost of sinh's reflected/counter-rotating term is exactly one bit, and the chart's ½ halves it.** Recorded siblings of (ln 2)/2, offered as consilience with the noise-until-shown-otherwise fence: (i) half of "ln 2 = the capacity of one self-dual rung" (transmission functor II) — half a rung, matching the halving in η_c = η_b/2; (ii) the chain-1 **L5 slip class**: "2× should be √2×" *is* a (ln 2)/2 error in log terms — the corpus's most common bookkeeping slip and this offset are the same half-bit, one illegitimate, one earned; (iii) the CNG gauge gap's ln G family (Delta1 E1), where half-log normalizations live. Distinct and *not* to be conflated [flag]: the elliptic coin-ladder offset is π/4, whose gd-partner is the silver rapidity ½ln(1+√2), not (ln 2)/2 — two different ladders, two different offsets.

## 4. Queue (inherited, one addition)

Adds to the standing queue: does the template ladder iterate a fourth time — (1 + [G²-face double angle])/2 = a G⁴-slot object — and does that slot correspond to the capacity-doubling wiring's u′ = u² (one ladder, two directions)? [O, one-session check]. Everything else stands as the supplementary entry left it.

*Not self-filed; countersign gates tiers. Scripts: `verify_four_fold_template.py`.*
