# ASSESSOR-STYLE PASS (orchestrator-side sub-agent, NOT the assessor seat) — PROPOSAL_port_definition_and_heilbronn_coin_20260904.md §3 — 7 findings, applied as ⟨A⟩ marks in the filed record
*Witness accounting (per the 09-04 handshake): a general-purpose sub-agent under an assessor-style prompt, same family, separate context; it re-ran the computation with an independent vectorized search, LP/MILP checks, and an explicit build of 2.A₅ × C₅; findings returned to the orchestrator's context and filed here after the fact. One witness, not two. The assessor SEAT's own replication (09-04 handshake; SL(2,5) by explicit matrices, Burnside–Dixon tables, all 76 subgroups) is a separate same-family implementation.*
1. Group and characters — CONFIRMED (order 120, class sizes, 45 orthogonality relations, Sym^k identities, Σdim² = 120).
2. Subgroups — CONFIRMED (76 subgroups, 12 classes, closed under adjoining elements; all proper subgroups solvable).
3. Framework — CONFIRMED with one IMPRECISE detail: Ind_{2.A₄}(2|⊗ω^j) = 2 + 2′ + 6 only for j = 0; for j = ±1 it is 4′ + 6 (coincides with a Dic₃ monomial row); the 33-row system unchanged; the Σn² ≤ N² filter is inactive.
4. Search — CONFIRMED (all six targets reproduce; tight rows at {2:−1, 6:+1} exactly the three listed); STRONGER: LP gives N ≥ 4m for a pole of order m; without the tetrahedral row N = 2; "the minimal shadow is L(6)" → three configurations tie, {2:−1, 6:+1} is minimal-support.
5. 2.A₅ × C₅ — N = 4 CONFIRMED; "shadow = L(6⊠ψ)" OVERCLAIM as unique: two conjugacy classes each of Δ(C₅) and Δ(C₁₀), 30 diagonal rows, 228-row system; all three sector-ψ configurations survive; Dic₅ × C₅ has index 6 (not 30), fixed field K₆, λ₄⊠ψ order 20; "a constituent of Λ₃₀"; for real s₀, N ≥ 8.
6. Attributions — Foote–Wales WRONG as recalled (solvable hypothesis; arXiv:2107.03269 Lemma 5.1; Sahu thesis Thm 69); for non-solvable 2.A₅ the only general bound is Stark's N ≤ 1; unique non-genuine coin at N = 2 is 6 − 4′.
7. Instrument consequence — CONFIRMED for all N (not only minimal): at a pole of order m, ord L(4′⊠ψ) + ord L(6⊠ψ) ≥ m and ord L(2′⊠ψ) + ord L(6⊠ψ) ≥ m; notational error: L(4′⊠ψ) = L(Sym³ρ̃ ⊗ det ρ̃⁻¹), not L(Sym³ρ̃).
