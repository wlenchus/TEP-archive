# AUDIT — [T]-chain status of the recent claims under the citable linchpin; the diagonal probe (data half); the inverse-factor reduction
*2026-08-29 (late), orchestrator, on Will's request ("reinspect which of our recently-highlighted potential claims might stand as an unbroken chain of derivations and correspondences at [T]") and his on-credit continuation. Offered, not self-filed. Scripts: `diagonal_probe.py`. Assessor pass covers this record with the day's stack.*

## 1. The linchpin, resolved better than requested

Will's reframe ([stated]): completeness↔sufficiency was wanted as the *linchpin* grounding all correspondences made across those lines — the single axiom converting the chain to a derivation; citability is good news. The audit sharpened this into a theorem-grade resolution:

**The keystone observation.** On the radial/equipartition sector — where d_eff lives, and which Will's own Petz-radial note just proved is the *universal* sector (g_rr metric-independent; Petz–Sudár 1996) — the framework's statistical models are one-parameter **full-rank exponential families** (the Bernoulli/binomial family p = ((1+r)/2, (1−r)/2); the multinomial equipartition families of the d_eff gauge). For full-rank exponential families, the completeness of the natural sufficient statistic is a classical theorem (Lehmann–Scheffé setting), and with Bahadur's theorem the whole sufficiency–completeness–minimality triangle *coincides*. **Consequence: on the radial sector, the linchpin holds as a citation, in the strong (equivalence) form, with no new axiom.** Off the radial sector — angular/Petz-fiber crossings — the monotone-metric multiplicity breaks uniqueness, and any correspondence routed there needs either the declared axiom or a chosen-metric declaration. This yields the audit's criterion:

> **Radial-sector crossings inherit a fully citable linchpin (Chentsov + Petz–Sudár radial + exponential-family completeness + Bahadur); angular-sector crossings need the axiom.**

The convergence is worth savoring: Will's Petz-radial note (proving d_eff is the moduli-free coordinate) is exactly what confines the linchpin's load to the sector where the linchpin is classical. The framework's own newest result closes its own foundational loop.

## 2. The [T]-chain ledger (recently-highlighted claims, per the criterion)

**Unbroken [T]/[T-cited] chains (derivation-grade as they stand):**
- d_eff identities (definition, SNR form, inversion isometry, quarter-turn G(1/x) = ixG(x), δG = δη, Shannon/Nyquist welds at d_eff = 2) — [T], verified this session.
- Petz-invariance of d_eff and of the radial crossing γ* — [T-cited] via Petz–Sudár; **note 2's §3.1 claim now stands as a cited chain**, not an assertion.
- γ* = sn²(K/2, k² = ½), companions cn² = tan(π/8), dn² = k′, Liu pair = (cn⁴, 2cn²) — [T], exact special values, machine-verified.
- The ad⁰ parity-port card (TW defect = dim H⁰(Gal_ℝ, ad⁰): 1 odd vs 3 even; lift-blind) — [T] two-line representation theory + [T-cited] standard numerology role.
- Three-parity distinction; both Artin parities at Casimir ¼ split by K-type — [T-cited] classical.
- Primitivity closure (flat = monomial ∪ block-imprimitive ∪ tensor; closure via Blichfeldt; the 2-dim incompressible; wall = primitive ∧ non-solvable) — [T-cited] + [T] (subgroup checks).
- Plancherel values (d_eff(χ₅) = 12/5; the double-cover spin sector at exactly d_eff = 2) — [T] arithmetic.
- Flat-floor tower F1–F3 (ρ̃|_{K₆} dihedral-automorphic; ρ̃|_{K₅} tetrahedral-automorphic; the cuspidal two-tile; L(ρ̃)L(ρ̃⊗χ₅) = L_{K₁₂}(ξ) entire) — [T]/[T-cited] chain, **assessor-pending** on the orchestrator's steps.
- Theorem Ω1 (COH ⇒ strong Artin) — [T] at record grade. Ω2 — skeleton-grade through L3, marked.

**Chains with a named weld (the weld, not the chain, is the open item):**
- Lemniscatic *forcedness* ("the special values are forced by the elliptic structure") — breaks at [C]: "the budget's elliptic interpolation is forced" is unproven; the identity layer above is [T].
- "Plancherel weight = signal fraction" — an [id] gauge declaration (legitimate as instantiation, not a theorem).
- Port/flat/anholonomy *language* on items 4–8 — [id]-dress over [T] content; harmless where declared, per the two-register statement.
- The functor syllogism P1/P2/(P3) — breaks at (P3) by the map's own design: (P3) is the conjecture.
- d_eff §5.4 operator prose — broken (the S′ = −θ′/π catch); the GMC half promotable (Saksman–Webb).
- Completeness↔sufficiency as a *general measure-space equivalence* — still would-be-novel; unnecessary for radial-sector chains per §1.

**Instrument class (certified-numerical, not [T], correctly so):** the three-tier COH exhibit; the diagonal probe below; the renderings.

## 3. The diagonal probe — data half (run this session, both conductors)

Class equidistribution on the certified primes to 10⁵: both datasets match Chebotarev weights (1, 15, 20, 12, 12)/60 to ~1% (the rare 1A to ~4–12%, consistent with its count); per-class |a_p|² equal the class values exactly (internal consistency). **Global RS mean: ⟨|a_p|²⟩ = 0.9948 (1951), 0.9929 (2141)** against the irreducibility prediction 1 — the Rankin–Selberg diagonal confirms on both certified datasets. Extended coefficients: Σ_{n≤X}|c_n|²/X stabilizes at **0.459 ± 0.003** across X = 10³…10⁵ (1951) — clean linear growth; the constant is now a preregistered target for the exact L(1, Ad)-side computation (the PARI half, queued: the R2 ledger gives L(1, Ad) = L(1, χ₅)·(Res ζ / Res ζ_{K₁₀′}) effectively). One convention flag for the assessor: the 5A/5B class labels carry opposite golden values in the two CSVs (mirror nomenclature between fields, values self-consistent) — confirm against the construction records before any cross-conductor class-level statement.

## 4. The inverse-factor reduction (the named obstruction, worked)

Setup: c = a ∗ β (Dirichlet convolution), a = coefficients of the abelian L_{K₁₂}(ξ) (degree 12, |a(n)| ≤ d₁₂(n)), β = Dirichlet-inverse coefficients of the cuspidal L_{K₅}(Π₅⊗ω) (degree 10, unitary Satake ⇒ |β(n)| ≤ d₁₀(n)). Then exactly:

Σ_{n≤X} c_n c̄_{n+h} = Σ_{e,e′≤X} β(e) β̄(e′) · Σ_{m: em≤X, e′ | em+h} a(m) ā((em+h)/e′)

— dilated, progression-constrained shifted convolutions of the *abelian* sequence with itself, weighted by β-pairs. **Proposition C1 (statable now; proof standard-but-owed):** the β-tails (e or e′ > E) truncate with effective error using the zero-free region of L_{K₅}(Π₅⊗ω) — available at standard effective grade because Π₅⊗ω is cuspidal and (order-5 determinant) *non-self-dual*, the Siegel-free case — mirroring, on the cuspidal tile, the R2 record's no-Siegel theorem on the abelian tile. **Consequence: NC for the ratio-object reduces, with effective error, to h-averaged dilated shifted convolutions of abelian norm-form sequences at degree (12,12)** — where the Molla-2026 averaged threshold reads H ≥ N^{5/6}, met by the mass route's H ≍ N. **The named target C2** (the residual gap, replacing "inverse factor" entirely): extend Molla-genre h-averaged bounds from full-level cuspidal pairs to level-bearing *abelian-induced* (isobaric) pairs — a more structured object than the original (norm-form lattice-point structure; Voronoi over number fields), and plausibly easier, not harder, than the cuspidal case it replaces. Odds register: unchanged in class (real, modest); the problem-shape is now strictly more ordinary than at any prior pricing.

## 5. Standing queue after this record
Assessor pass (whole stack, plus the 5A/5B convention flag); the PARI half of the diagonal probe (L(1,Ad) exact constant vs 0.459); C1 written to proof; C2 scoped against Molla's method; then the deposit work resumes from credit per Will's word; the map's §9 calls hold their places.
