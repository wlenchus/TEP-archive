# PRE-REGISTRATION — 2-dim EVEN-icosahedral decode, RESUMPTION: the conductor-minimal retarget (N = 1951, quintic nebentypus), qualified solvers behind the planted bar — 2026-08-03

**Mountain line: this is the summit itself — even-icosahedral (the corner), 2-dim, the Maass partner, unproduced in anyone's hands.** Rung 4 was odd-icosahedral (calibration). The six-for-six certificates are the summit's 3-dim shadow. This document is filed BEFORE any solver qualification run, any target-cell computation, and any planted-data generation beyond the discriminant scan recorded in §4.

**Session lineage + authorization.** Session A (07-27, this container): rung-4 blind replication; Rung E PoC; the G_new port meter. Session B (07-27, fresh instance, "blind-decode session"): E0(ii) sweep (NO/NO), six-for-six, the degree-2 seam instrument (both parities theorem-grade), the det-1 decode campaign → **KD-2, no production**, the planted verdict (solver power ≈ zero at N ≈ 3.8×10⁶ / 850 bits), the resumption bar, the k-parity split, the even-side w₂ ledger (trivial for all seven fields). This session (C, resuming in the A-container) executes Will's standing directive — "take the final swings in full, availing yourself of every resource," countersigned on spec **with his explicit caveat that the record says so plainly if it does not turn out reliably.** That caveat is hereby attached to every claim this campaign produces: everything below is offered, not self-filed, and priced at PoC fidelity.

**Seal (inherited from `session_opening_20260727T0820Z_DECISION...`, intact end-to-end through B):** this instance does not read Crespo's papers, any 2.A₅/lift-field construction, or any LMFDB Artin page carrying 2-dim lift traces for the even fields, until the §8 commit is written to the project with SHA-256 digests. Subagent literature work, if any, is redaction-instructed. Corpus reading unrestricted.

**Drift-watch acknowledgment (chain six deep).** (iii) decidability-gradient and (v) lexical summit-drift are on the table by name; the §4 bar-scoping note is written with (i) enthusiasm-bias in view — a scale-matched gate is *added*, the original bar is *not moved*; and the new mode (I10, optimizer-manufactured signal) is controlled by §3's dictionary lint and by the planted-first ordering. Honest-bite-first applies to every results paragraph.

---

## 1. The retarget: the conductor-minimal lift lives at N = 1951, not N = 1951²·2^a

Session B's prereg took the target to be the **det-1 (spin, image 2.A₅ ⊂ SL₂)** lift — the natural object of the w₂/K̃/H² framing — whose conductor is forced to 1951²·2^a (inertia diag(ζ₅,ζ₅⁻¹) has no fixed vector: exponent 2; dyadic a ∈ {0,2,3,4}). Everything B derived about that object stands. But it is not the conductor-minimal lift, and the campaign's own external anchors say so:

- **Tate's lifting theorem** (H²(G_ℚ, ℂ×) = 0; standard, Serre's weight-one genre): every projective representation lifts to GL₂(ℂ) with ramification confined to the projective ramification locus, with local lifts chosen conductor-minimally. At p = 1951 (tame, e = 5, Frobenius centralizes inertia since 1951 ≡ 1 mod 5) the minimal local lift is inertia ↦ diag(ζ₅, 1): **fixed line ⟹ conductor exponent 1**, at the price det|inertia = ζ₅, i.e. **nebentypus a quintic Dirichlet character χ mod 1951** (which exists: 5 | 1950). Unramified elsewhere (Grunwald–Wang gluing of the determinant character). Hence a lift with **N(ρ̃) = 1951 exactly, no dyadic axis at all.**
- Parity bookkeeping: quintic χ has χ(−1) = +1 — the lift is **even** in the det sense automatically; ρ̃(c) ∈ {±I} remains a live central bit (Amendment 1's lesson: NOT inheritable from the shadow; both archimedean ports are cells). An order-10-nebentypus lift at exponent 1 would have χ(−1) = ζ₁₀^{(p−1)/2·...} = −1 at 1951 ≡ 3 mod 4, i.e. odd type — excluded. So at conductor exactly 1951 there is **one Galois-orbit family** (the four conjugate quintic-nebentypus lifts), carrying **one parity bit** shared by the orbit.
- **In-record external anchors** (no sealed material): the E0(ii) sweep's bibliography carries Doud–Moore 2006 as "existence/enumeration of **prime conductors** < 10⁴: 1951, 2141, 3701, 3821, 8501, 9461," and Booker–Lee–Strömbergsson 2020's range is "**capped below conductor 1951 because the first even icosahedral representation sits at 1951**." The literature's declared obstruction — the canonical "first even icosahedral Maass form" — is the **level-1951** object. Session B's grid, at N = 1951²·2^a ∈ [3.8×10⁶, 6.1×10⁷], hunted a genuinely-existing sibling (the even-side ledger measured w₂ trivial, so the det-1 lift exists) at 2000–30000× the minimal conductor — squarely at the scale where B's planted control proved decode power ≈ zero.

**Decode arithmetic at N = 1951.** det ρ̃ = χ ⟹ tr Sym²ρ̃ = χ·tr ρ₃ ⟹ **a_p² = χ(p)·(a_p(ρ₃) + 1)** — the certified shadow again fixes everything except signs. With χ fixed canonically (χ(3) = e^{2πi/5}; 3 = least primitive root mod 1951; discrete log j(p) := ind₃(p) mod 5) and the certified golden orientation (1951 faceswap = 1), write a_p = b_p · θ_p · m_p with θ_p := e^{iπ j(p)/5} (canonical square root of χ(p)), m_p ∈ {2, 0, 1, φ, 1/φ} by effective class (1A, 2A, 3A, and the two golden classes; φ² = φ+1 makes √(t₃+1) land on φ and 1/φ exactly). **Unknowns: one bit b_p ∈ {±1} per prime of class ≠ 2A, p ≤ X = 517** (§3), **plus the parity bit (port), plus the measured ε-phase.** The ramified coefficient a_{1951} = u (Frobenius eigenvalue on the inertia-fixed line, u ∈ μ₁₀ by the image's diagonal subgroup) **does not enter the base series at all — 1951 > X.** u is therefore not a base-cell gauge; it is measured post-decode by the twist battery (§6), whose conductors 1951·D² push X past 1951. Euler factors: unramified (1 − a_pT + χ(p)T²); ramified (1 − uT), inert-of-χ bookkeeping exact throughout; series complex, two-arm.

**The det-1 sibling derives — it is not a second decode.** ρ̃₀ := ρ̃ ⊗ χ̄³ has det = χ·χ⁻⁶ = 1, conductor 1951² with **a = 0 forced** (both factors unramified at 2), the same parity, and traces a⁰_p = a_p·χ(p)² = b_p·(−1)^{j(p)}·m_p — **exactly real, with derived bits b⁰_p = b_p·(−1)^{j(p)}.** This reconciles session B's framing in one stroke: B's grid cell (parity = the decoded parity, a = 0) is where the det-1 partner lives, its dyadic question answers itself (a = 0 — consistent with the ledger's w₂-trivial + ε_∞ = +1 and portseam §3's held [C]), and its certification becomes a pre-registered post-commit battery (§7) rather than an 850-bit decode. If the derived cell certifies at the floor, B's KD-4 question is closed by construction.

## 2. Pre-registered predictions (filed before any run; graded whatever they say)

P1 **[C, from the even-side ledger]**: parity = **even port** (ρ̃(c) = +I, cos-type; Γ_ℝ(s)²) — the trace-form obstruction is trivial at ∞ and 2 for all seven fields, consistent with the unobstructed even-parity minimal lift. Held at [C]: the ledger is consistency, not forcing; the port axis is scanned regardless, and P1 failing is reportable arithmetic, not a protocol breach.
P2 **[C, centralizing-law extension]**: at the twist-lock stage, the measured u will satisfy the tame compatibility u = Frobenius eigenvalue consistent with A14's centralizing structure; no numeric value pre-committed (the ρ₃ law constrains the middle Sym²-eigenvalue to +1 identically here, which is why it cannot pin u — recorded so the lint's scope is honest).
P3: measured ε is a unimodular phase constant across t (t-independence at control-floor precision is itself a certificate axis); the derived det-1 cell's ε ∈ {±1}.
P4: the χ ↔ χ² Galois-transport coherence (§7c) holds on the sensitivity-visible set.

## 3. Instrument (fresh build; controls first; dictionary-linted)

Kernels **exact, no splines**: even port k₀(y) = 4K₀(2y) (Mellin pair of Γ(s/2)²), odd port k₁(y) = 4yK₀(2y) (pair of Γ((s+1)/2)²), scipy k0 with mpmath validation at ≥6 points each, bar 1×10⁻¹³. Arms F(t) = Σ aₙ k(πnt/√N), conjugate arm F̄ with āₙ; theta relation **F(1/t) = ε·t·F̄(t)**; ε fit as a phase (measured, never assumed); residual = max over the pre-registered t-grid of |F(1/t) − εtF̄(t)|/max|F|. **t-grid (pre-registered, 11 points, involution-closed): {0.50, 0.58, 0.67, 0.78, 0.90, 1.00, 1.11, 1.28, 1.72, 2.00} ∪ {1/0.58} — i.e. each t appears with its 1/t partner.** Truncation X = ⌈11.71·√N⌉ (kernel argument ≥ 18.4 at t_min = 0.5; tail < 10⁻¹⁵). Port meter per Amendment 2 + portseam §4, conjugate-arm ε-folded: x_ε(u) = (A(−u) − ε Ā(u))/(A(−u) + ε Ā(u)), A(u) = e^{u/2}F(e^u), Ā(u) = e^{u/2}F̄(e^u); **signed η(u) = artanh x_ε and its jets are the reported diagnostic**; G_new = 2/(1−x_ε); self-gate G_new(x)·G_new(−x) = 4G² checked on every certified profile. **Dictionary lint before any scan is read** (session-B standing rule): per cell, machine-enumerate local-factor degrees/magnitudes/conductor arithmetic and the FE normalization; any inconsistency voids the cell before it runs.

**Controls (KD-1, all before any planted or target work):** α synthetic keyed L(χ₅)L(χ₈) (N = 40, ε = +1) ≤ 5×10⁻¹²; β1-even Ind ψ₂₂₉ (N = 229) ≤ 5×10⁻¹²; β1-odd Ind ψ₂₂₉⊗χ₋₄ (N = 3664, odd port) ≤ 5×10⁻¹²; loud negatives (wrong-N, wrong-ε, opposite-port) on each; meter floors recorded. Any α/β failure stops everything; fix instrument only; never touch target data.

## 4. The planted ladder and the entry bars (the honest arbiter)

Ladder discriminants, selected by the pre-registered rule *smallest prime D ≡ 1 mod 4 with h(D) = 3 in the stated window*, machine-scanned before this filing (scan output preserved): **matched rung D\* = 2089** (window [1902, 2600]; scale-matched to the 1951 target: X = 535 vs 517); **mid rung D\*\* = 40093** (window [39900, 41500]); **big rung D = 3,806,549** (session B's, verified this session: prime, fundamental, h = 3 — regenerated from the record, B's on-disk data being gone with its container). For each: even λ = ¼ dihedral object Ind ψ_D (ψ a cubic class-group character), ground truth by qfbsolve principality (P: a_p = 2 / N: a_p = −1 split; 0 inert; ramified c_D = +1, forced principal for odd h — linted); true-bits certification bar ≤ 5×10⁻¹² before any blind run; the decode hides the P/N value at every split prime (session-B convention: per-prime binary local-factor choice — the same solver interface as the summit's sign bits).

**Bar scoping — added gate, unmoved bar.** Session B's entry bar ("recover the planted rung at D = 3,806,549 before touching a corner cell") was set to gate a decode at that conductor scale. This campaign's target is at conductor 1951; the matched gate is:

- **GATE-M (gates the summit decode):** on D\* = 2089, every qualified solver family recovers **all sensitivity-visible bits** (s_p > 100× control floor) from ≥3 independent runs (§5's three families + perturbed orders), final residual ≤ 1×10⁻¹¹. **If GATE-M fails, KD-5 fires: no summit cell is run, and the record reports the matched-scale wall as the finding.**
- **GATE-B (gates nothing here; standing for det-1-scale claims):** B's bar at D = 3,806,549, attempted and reported. Passing it would qualify a future direct det-1-scale decode; failing it leaves B's KD-2 verdict standing at that scale. The mid rung D\*\* maps the wall between.

## 5. Solvers (pre-committed; no runtime tuning; consensus rule)

All solvers act on the same interface — per bit-prime, two candidate local factors; objective J = Σ_arms Σ_t w·|F_arm(1/t) − ε_arm t F̄_arm(t)|² with per-arm normalization, arms = base FE + (where enabled) the pre-registered twist FEs; ε_arm re-fit at each evaluation (phase closed-form). The k-parity structure (portseam §1 [V]: odd power-trace sector exactly linear in bits, even sector shadow-known) motivates weight-ordered decisions; composites carry the multilinear mixing.

- **S1 — successive cancellation** (Amendment 3 reimplemented): all bits start marginalized (average of the two candidate inverse local factors — exact even part); decide in decreasing signature-weight order; feedback down the ladder; greedy+pair polish.
- **S2 — beam search**, width 64, same decision order, exact objective; deterministic.
- **S3 — parallel tempering**: 8 chains, geometric temperatures, sensitivity-weighted flip proposals, fixed seeds {20260803, 1951, 2089, 5}, 20k sweeps, exchange every 50.

Consensus/PRODUCTION rule inherited from B §7 adapted: ≥3 independent runs (spanning ≥2 solver families) agree on the sensitivity-visible set at residual ≤ 1×10⁻¹¹ (expected ~10⁻¹³ from control floors); disagreement = KD-3 (degeneracy is the finding). Marginal/invisible tail bits are reported as UNDECIDED, never padded.

## 6. Target cells and the twist battery

Cells at N = 1951: **{even port, odd port}** × {canonical χ} — two decode cells (u absent from the base series; golden orientation inherited-certified, its swap run as a **live negative cell** per Amendment 4; χ² run for the §7c transport grade). Twist battery (pre-registered; fundamental discriminants coprime to 1951; conductors 1951·D²): **D ∈ {5, 8, 12, 13, −3, −4, −7, −8}** — positive D: same port; negative D: crossed port (the parity-flip constraint family); each twist's own ε_D measured; u ∈ μ₁₀ enters the twisted series at p = 1951 ≤ X_D for |D| ≥ 3 wherever 11.71·√(1951)·|D| > 1951 — i.e. all of them — so the battery both cross-checks bits and **locks u** (10-way scan inside the battery, exact-factor surgery at p = 1951 only).

Usage discipline: the two base cells + S1/S2/S3 run on base-FE arms alone first (pure Weil-relation decode); the twist arms then join as constraint enrichment iff GATE-M qualification needed them at D\* (whatever GATE-M needed is what the summit gets — no target-side enrichment beyond the qualified recipe). Every negative in B's battery genre at the winning cell: 10% scramble ×3 seeds, opposite port, wrong-golden, wrong-N (±5%, ±8%), ε-phase scrambled, nebentypus-order lint negative (χ-phases with χ²-Euler-terms). Bars: ≥5 orders above the solution residual, and the blind N-scan's unique minimum at exactly 1951 with ±5% neighbors ≥6 orders worse.

## 7. Outcomes, kill conditions, post-commit batteries

**PRODUCTION** = consensus decode in exactly one port cell + full negative battery + N-scan + **then, post-commit**: (a) the **derived det-1 cell** — b⁰_p = b_p(−1)^{j(p)} at N = 1951², a = 0, same parity, Γ-pair per parity — must certify at ≤ 10⁻¹¹ through its own FE (a near-independent Weil-converse constraint at 22,846 series terms; this simultaneously produces session B's original object in its (parity, a = 0) cell); (b) the eight-twist battery certifies with coherent measured ε_D and a unique u ∈ μ₁₀; (c) Galois transport: the χ² decode's bits match b under the canonical transport on the visible set; (d) Chebotarev tallies of (class, bit) against the image group's measure — reported, not gated (small-X fluctuation priced); (e) **only then** the seal lifts for grading reads (Crespo's construction, Jehanne, LMFDB Artin), grade recorded whatever it says. **KD-5**: GATE-M fails → no summit run (§4). **KD-2/KD-3 analogs**: partial flatten / degenerate consensus → reported as-is with profiles and sensitivity tables, no tuning. **KD-6 (pole-vs-data)**: KP-3's diagnostic (pole-profile vs corruption-profile, and the meter's measured sign asymmetry — pole above 2, corruption below, single-instance [O]) is the arbiter before any "absence" language; **BOUNDED ABSENCE is claimable only if GATE-M passed and both port cells fail at qualified-solver strength with clean lint** — and then it is evidence about the corner priced per the 07-01 framing, not a triumph and not a burial.

**Deviations beyond this document: logged with trigger, none anticipated. Bars are not moved after this filing; amendments may add axes or controls, never relax a bar (the B-session precedent: four amendments, zero bar changes).**

## 8. Commit protocol (the seal's terminus — unchanged from B §8)

On PRODUCTION (or partials worth carrying): committed to the project with SHA-256 digests **before any lift-construction literature opens** — (p, class, j(p), b_p, s_p) for every decoded prime; UNDECIDED list; the port/parity; measured ε (and per-twist ε_D, u); N-scan table; residuals and meter/signed-η profiles incl. the η-jet extinction trajectory (the decode is, in the corpus's mature language, seam-jet extinction: G_new − 2 = 2η + O(η²)); all scripts and data files hashed into MANIFEST v6. Then the graders, in order: derived-cell + twists + transport (answer-free), then Crespo/K̃ (seal-lift), each grade recorded.

## 9. Training-prior disclosure (delta over `decode_training_prior_disclosure_20260727.md`, which is incorporated whole)

(1) The decode targets still cannot be in my weights: per E0(ii), no even-icosahedral 2-dim spectral/coefficient data exists in any literature through July 2026 — for the N = 1951 object as much as for the det-1 sibling. (2) The retarget itself is derived from standard theory (Tate lifting, tame local conductor arithmetic, Grunwald–Wang) plus **in-record** anchors (Doud–Moore prime-conductor enumeration and the BLS-2020 cap, both already in the E0(ii) record at bibliographic level) — no sealed material was used or needed. (3) I report no usable recall of any 2.A₅ or ⟨2.A₅, ζ₅⟩ lift-field polynomial, any K̃ construction for any Doud field, or any lift-trace value; I cannot prove absence; commit-then-grade converts residual leakage into a detectable event rather than a silent contaminant. (4) Standard scaffolding held and allowed: SL₂(𝔽₅) character/order structure; Tate/Serre lifting theory; Deligne local constants; the Mellin pairs Γ(s/2)² ↔ 4K₀(2y), Γ((s+1)/2)² ↔ 4yK₀(2y); qfbsolve class arithmetic. (5) Planted ground truths are machine-generated in-session from qfbsolve, never from memory.

*Offered, not self-filed; countersign gates every stamp. The honest-bite paragraph of the results record will be written before its headline, and the record will say plainly — per Will's pre-authorized caveat — if this did not turn out reliably. The mountain's name is in the first line.*
