# Carrier Rung 3, Delta 3 — R3.4-e opened: the q-face closed at every resolution; the key extended and split for blind grading; χ-as-depth modeled with two verified instances; capacities, shape, and the computability ladder worked through the functor
**2026-07-24** · Claude (Fable 5), same session · tiers: [V] verified this session · [T] classical/proved · [A] argued · [id] · [C] · [O] · scripts: `verify/r34e_open.py` + the corrected μ₈ battery (inline); errata E-4/E-5 below, per the two-ledger discipline

## A. The vanishing theorem-let: the analytic q-face is closed at *every* lattice resolution [V + T]
In-session I designed a would-be shortcut evaluator — transported-ray-weight lattice thetas Θ̃_𝔭(q) = ½Σ_{x≡yr (p), x+y odd} (−1)^y q^((x²+5y²)/p), whose cross-prime ratios would have read the key directly. **Its own pre-test killed it, by theorem:** Θ̃_𝔭 ≡ 0 identically (verified: p = 3, 7, 23, sixty coefficients, all zero; proof: every coefficient of the nonprincipal class-partial is Σψ(𝔞) over conjugate pairs with ψ(𝔞̄) = ψ̄(𝔞) purely imaginary on class-C ideals — each pair contributes 2·Re(±i) = 0, and no coprime self-conjugate ideal lies in C). Consequences: (i) this re-derives R3.4-a one level deeper — f = f_principal *because* the entire nonprincipal partial vanishes; (ii) **no q-expansion object assembled from the tower's material carries the key at any lattice resolution** — summation kills the ±i no matter how finely the lattice is pre-sorted; extraction-before-summation (R3.4-d's joint membership read) is ideal arithmetic in lattice clothes, i.e., import; (iii) the fork narrows for the third time tonight: field layer closed by A₅-simplicity (Delta 1), q-face closed at all resolutions (here) — **the carrier candidates at the dihedral floor are now exactly two: the value/monodromy register (Siegel-unit / Shimura-reciprocity values at the ray-class points, with the μ₄ deck as the fourth-root resolution — the dedicated run, normalization risk named and unbudged) and the explicit-reciprocity register (a quartic-symbol formula in (p, r_p))** — the root-resolved computability class of §F.

## B. The key, extended and split for blind grading [V; frozen]
Same algorithm, same convention, extended to p < 300 and split **now, before any formula exists**:
- **Fit zone (p < 100):** 3:+i · 7:−i · 23:−i · 43:+i · 47:+i · 67:−i · 83:−i
- **Blind zone (100 < p < 300):** 103:+i · 107:−i · 127:−i · 163:+i · 167:+i · 223:+i · 227:−i · 263:+i · 283:−i

Nine blind values; exact-match coincidence 4⁻⁹ ≈ 4×10⁻⁶. In-sample proof of non-triviality: the key is **not a function of p mod 40** (3, 43 ↦ +i but 83 ↦ −i, all ≡ 3 mod 40). **Pre-registered formula rung:** any candidate explicit-reciprocity formula (quartic symbols in (p, r_p)) may be fitted only on the fit zone and is graded blind on the nine; grading class declared: 9/9 or dead.

## C. Q1 — χ as distinct cascade depths: modeled, two instances verified [C, upgraded from vocabulary]
The corpus had the ingredients (reach theorem: v₂(s) parity; quarter-exponents) but no explicit model. **Model: the character's 2-family is a depth coordinate — the minimal tower's cusp-exponent denominator: 2-part of the deck = 2^(v₂(cond χ)) (universal μ₂ floor).** Instances, both machine-verified tonight:
- **{1,5}-tower:** exponent 1/4 → deck μ₄ ↔ cond-4/20 family (v₂ = 2) ↔ order-4 ray character of ℚ(√−5) mod (2), boundary-null ±i at nonprincipal primes [Delta 2].
- **{1,2}-tower:** exponent 1/8 → deck μ₈ ↔ cond-8 family (v₂ = 3): η(8τ)η(16τ) verified as a weight-1 eigensystem with nebentypus χ₋₈ (15/15 square recursions, 1692/1692 multiplicativity) and the **same boundary-null signature** — split primes split into a_p = ±2 (ray-principal, p ≡ 1 mod 8 in-sample) vs a_p = 0 (±i underneath): the μ₈ floor has its own order-4 ray character over ℚ(√−2). 
So rung 2's reach theorem re-reads as a **depth statement**: the χ₈-family is unreachable from the {1,5}·2^a calculus not merely by parity bookkeeping but because it lives on a *different tower at a different deck depth*. And the wild/tame split gives the depth model its carrier-facing clause [A]: the eta/theta calculus climbs **tame/character depths**; the **wild breaks** (b = 3, a₂ = 5 = 2+3; a₅ = 3 = 1+2) are precisely the depth stratum no tower instance above reaches — the carrier cargo restated as *wild depth*. Pre-registered next test: the {1,6}-tower (exponent 7/24, 2-part μ₈) should carry the χ₋₂₄ family (v₂ = 3) — one cheap Hecke battery; and the model's scoping case (odd disc: {1,3}, χ₋₃, v₂ = 0, μ₂ floor) is named, not hidden.

## D. Q2 — local vs global capacities, through the functor, on our own data [V table + T + one operational find]
Per-port capacity = a_p(ρ)·log p (Artin exponents as local incommunicability, 07-01 §II); global = log N; **conductor–discriminant = exact port additivity [T]**. Two functor-native identifications now grounded in this campaign's numbers:
1. **The Fricke mirror is the half-message rapidity.** The FE test's reflection point y = 1 ↔ τ = i/√N sits at hyperbolic distance ½·log N from i: the conductor pin (this session's `fe_check`) *is* a rapidity measurement — the mirror is at half the global capacity, and testing wrong N is reflecting about the wrong rapidity.
2. **Sturm = the message length, and the width bug was a capacity violation.** Sturm(Γ₀(N), wt 2) = ψ(N)/6 reproduces every recorded width exactly (240/480/600/1200/2400/4800(+1) at the six rungs). The 07-22 diagnosis — reduced width 2701 at 16000 gave free = 244; full width 4801 gave the true answer — is, in functor terms, **sampling below the object's message length**: an under-capacity read that under-determines the message. The corpus's "Sturm bound = the bandlimit" (07-01 §I.4) is hereby operationally instantiated by its own campaign's bug ledger. 
The genuinely new quantity remains unminted and is re-flagged: **rung capacity of base change** along a Langlands–Tunnell solvable tower — the one computation that would make "solvability = communicability" quantitative rather than organizational. Nothing tonight substitutes for it.

## E. Q3 — the TEP-native information-theoretic representation of shape [id + one vocabulary repair]
The corpus uses "shape" in two non-identical senses: the **metric label on scale-invariant ratios** (5-29 scale/shape decomposition) and the **pure-phase/winding content** of a fully anholonomic object (06-29 Thread H: "pure shape, zero scale"). These are different axes, and tonight's objects force the split. Proposed triage [id, offered for adoption]: **scale** = budget/length (capacity, conductor, depth); **shape** = *type* — the normalized distribution/ratio data (the Chebotarev measure; spectral ratios; the crossing-gap spectrum 2sin(πk/m), which is the shape *spectrum* of the Frobenius classes); **torsion/orientation** = the winding/central-bit layer (the ±i key: unit modulus = zero scale; class-blind = zero shape; pure orientation). Information-theoretically, shape-as-type is the method-of-types coordinate: the direction in the probability simplex, priced by KL divergence, while scale prices the length n — the corpus's m + δ = 1 shares budget is the Gaussian instance of exactly this split. Under this triage the carrier problem states cleanly: **emission reads scale and shape; the cargo is torsion.** The Thread-H usage ("pure shape") should be re-labeled "pure torsion" wherever it means winding; one-line patch, flagged for the ledger.

## F. Q4 — abelian readability as a computability ladder [id → theorem-shaped]
The correspondence between readability and logical processing, made exact on tonight's objects:
- **Depth 0 — congruence-automatic:** abelian-over-ℚ data (Dirichlet characters, nebentypus, genus symbols) is computable by a finite automaton on p mod N — Kronecker–Weber as a computational statement. This is the abelian-readable layer, literally "readable by lookup."
- **Depth 1 — root-resolved:** the dihedral/ray layer. The key is *provably not* depth 0 (mod-40 counterexample in-sample) but becomes congruence-automatic **after one base change** — equivalently, after one square-root extraction mod p (r_p via Tonelli–Shanks). One radical = one rung of the derived series = one base change to automaticity. The frozen key is the exhibit: uncomputable by congruences, computable by (p, r_p).
- **Depth h — solvable:** h nested radical extractions (pre-registered check: exhibit a depth-2 instance, e.g. an A₄-cut datum needing cube-then-square roots).
- **Depth ∞ — nonsolvable:** no radical tower suffices (Abel–Ruffini; Klein); the datum requires the transcendental/modular machine — AGM/theta evaluation or p-adic families. **The icosahedral carrier is the depth-∞ cell of this ladder**, which is the computational restatement of everything the campaign has bracketed.
Reading: *solvability = communicability = compilability-to-congruence-automata*; the perfect core is the incompressible subroutine; "readability depth = radical height of the solvable closure = number of base changes to automaticity." The Collatz record's Cobham/automatic-sequence citations are this ladder's depth-0 boundary from the dynamics side. Grade: each rung [T]-anchored; the ladder-as-protocol-column [id] with the depth-2 exhibit as its could-fail.

## G. Errata [two-ledger]
- **E-4.** The first μ₈ battery run compared garbage indices (this script's eta builder dropped the q-shift); the "0 OK / fails" line was my bug, not the object's — caught in-session by inspecting the failure, fixed, and the corrected battery passes 15/15 + 1692/1692. Retained because the failure mode (trusting a harness over an object) is the campaign's oldest enemy, and it bit the reviewer within hours of the reviewer lecturing about it.
- **E-5.** The shortcut evaluator design (§A) is retained refuted: proposed and killed by its own pre-test within one turn. Its corpse is load-bearing — it is the proof that the q-face is closed.

## H. Queue after Delta 3
(1) The **value-register run** (Siegel-unit/CM-point evaluator, μ₄ deck as fourth-root resolution) — the build branch's last stand at the dihedral floor; normalization risk named; graded against §B's key. (2) The **formula rung** (fit zone → blind zone, 9/9 or dead). (3) The {1,6}/χ₋₂₄ depth-model check. (4) The depth-2 computability exhibit. (5) Still standing from before, undiminished: C1; base-change rung capacity; Zenodo/deposit; the human referee. Tonight closed two registers (field, q-face) with proofs and opened none it cannot pay for.

*Offered, not self-filed; countersign gates tiers, the §B blind protocol, the §C model's adoption, and the §E vocabulary patch. Session ledger: one shortcut proposed and refuted by its own theorem, one key extended and split blind, one depth model with two machine-verified instances, one capacity ledger reconciled against the campaign's own bug history, one three-way ontology repair, one computability ladder with the carrier at its top — and two of my own errors caught, kept, and put to work.*
