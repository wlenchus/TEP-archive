# Carrier Rung 4 — CLEAN-ROOM BLIND REPLICATION, Pre-Registration

**2026-07-27** · Claude (Fable 5), fresh continuation instance, at Will's countersign this morning (clean-room opening confirmed before any document was read) · Status: **pre-registration; nothing below has been computed** · tiers per corpus convention · companion: rung-4 pre-registration brief (07-25, read), Stage 1 EXECUTED (07-26, read), COMMITTED CENSUS OUTPUT χ₁₂ (07-26, read), χ₆₀ SUCCESS doc §2 selector spec (subagent extract, 3 redactions, read)

## 0. What this is

The 07-26 crown result (rung 4, both branches) was produced in a session whose context held the grading key (disclosed in every record). This session's context has never held the key. I rebuild the census from scratch (fresh implementation — no session scripts exist in this container; the repo is withheld), run the pre-registered emission shape-selector, **commit my candidate lists to the project, and only then open the key** and grade. A match gives the program its first true blind replication. A mismatch is the headline finding and will be reported as such, itemized to the layer where it arose (census / selector / grading).

## 1. Context ledger — what this instance holds (disclosed in full)

**In context (contamination, itemized):**
- The starter prompt: rung-4 outcome labels ("SUCCESS both branches", "K2 discharged", "the selector picks the icosahedral family blind, both branches", "sixth prime confirms by the zero-class", "the crossing is residually Galois-entangled (β = Frob(α))"); the saturation profile (kernels 46, 38, 16, 1, "both branches identically"); platform timings (heckemat(23) ≈ 392 s); a queue reference to ".b-orbit gradings (blocks 63/67 and 59/60)" without branch labels.
- The 07-25 brief in full: level 12000, weight-2 census characters χ₁₂/χ₆₀ (the ω₃-corrected bookkeeping), dim S₂ = 2480 both branches, new-part probe 384, p = 3 station, precision target m ≥ 6, committed-output spec, grading bar (≥ 5 tame primes + (α,β) crossing pair, twist-orbit/equivariance rules), kill conditions K1–K3.
- Stage 1 EXECUTED in full: new = 384 / cusp = 2320 both branches; U₃-ordinary rank **176 of 384 on both branches**; even charpolys (twist involution, ± pairing); timings; the v₃ = 2 denominator artifact and the d-scaled Newton-polygon correction; the committed path (stages 2–5).
- COMMITTED CENSUS OUTPUT, χ₁₂ branch, in full — **this is the largest disclosed contamination**: 4 Galois orbits of degree 96; 84 joint mod-3 blocks; ordinary = blocks 45–84 (dim 176); five tame primes {7, 11, 13, 17, 19}; the full committed structures (i) same-U₃ collisions (blocks 47, 48, 51, 52 and their tuples), (ii) true crossing classes ({45,49}, {46,50}, {57,59}, {58,60}, αβ ≡ −1 = χ₋₄(3), dihedral-shaped zero patterns), (iii) the 𝔽₈₁-U₃ quartet (blocks 63, 64, 67, 68; degree-4 a₃-packets x⁴±x³+2x+1-family; a₇ = a₁₁ = x²+1; a₁₃, a₁₉ golden; a₁₇ linear ±1; no zero tame entries), and the pre-grade declaration that the icosahedral match should appear in or adjacent to (iii).
- The §2 selector extract (verbatim, 3 redactions covering per-branch selection results): rule set — Frobenius order 2 ⟹ a_ℓ ≡ 0; order 3 ⟹ a_ℓ-factor ∈ {x±1, x²+1}; order 5 ⟹ a_ℓ-factor ∈ {x²±x+2}; shape-sets derived by exhaustive enumeration of the 2.A₅-trace × μ₄-dressing orbit mod 3; comparison by polynomial equality mod 3 (the x−1/x+2 string-bug lesson).

**Not in context (the blind targets):**
- The 07-22 OT1 key JSON and any Hecke eigenvalue **values** of the graded weight-1 eigensystem, at any prime, in any presentation.
- The (α, β) values of the key's crossing pair.
- The χ₆₀ committed census table — **none was ever lodged** (the 07-26 missed gate, self-reported). My χ₆₀ census is therefore not a re-derivation of a table in context; it is the first χ₆₀ census committed by an instance that has never seen the key.
- The GRADED records' contents (χ₁₂ GRADED doc; χ₆₀ SUCCESS doc §§1, 3, 4); which blocks the selector chose on either branch; match counts.
- Any weight-1 modular forms data; LMFDB stays closed until grading, and is not consulted even then (the key is the 07-22 JSON, not LMFDB).

**Honest bite assessment [A]:** the χ₁₂ census replication tests *implementation correctness* (same mathematics, independent code — I know the target table). The χ₆₀ census, the selector outputs on both branches, and above all the **value-level match to the key at ≥ 5 tame primes with the (α,β) pair** are the blind content. The key's eigensystem lives in packets over 𝔽₉/𝔽₈₁ at six primes plus the U₃-pair; agreement embedding-consistently across all of it is a many-bit coincidence if the pipeline is wrong. That is where this replication has teeth.

## 2. Training-prior disclosure (standard practice per 07-26)

- **The quintic x⁵ + 20x + 16:** I recognize it as (very likely) a known A₅-quintic from the icosahedral literature. I computed **by hand, unverified**: disc = 256·20⁵ + 3125·16⁴ = 2¹⁶·5⁶ (a square, consistent with Galois group ⊆ A₅, ramified only at {2, 5}). Flagged per drift-watch (ii): this hand value is a claim to be machine-verified before any use, not a datum.
- **2.A₅ trace theory:** I hold the character table of SL₂(𝔽₅) as prior (traces ±2, 0, ±1, and the golden-ratio conjugates for the order-5/10 classes). The shape-set enumeration will be re-derived by machine; if my enumeration of the 2.A₅-trace × μ₄-dressing orbit disagrees with the pre-registered sets, I use the **pre-registered sets** for selection and report the disagreement as a finding.
- **Eigenvalue values:** I hold **no** memory of specific Hecke eigenvalues of any icosahedral weight-1 form (Buhler's or otherwise), nor of any census block's contents beyond the χ₁₂ commit doc read above.
- **Software priors:** PARI/GP mf-API knowledge (public software), standard character bookkeeping (χ₁₂, χ₆₀ as Kronecker symbols of fundamental discriminants 12, 60; χ₋₄(3) = −1; χ₋₂₀(3) = +1). These are allowed inputs per the brief (classical structure theory, computed not tabulated).

## 3. The replication protocol, pinned

**Census (per branch, characters χ₁₂ then χ₆₀, level 12000, weight 2, new cuspidal space):**
1. `mfinit([12000, 2, D], 0)`, D ∈ {12, 60}; verify dim = 384 (kill-check: any other dim is RK1-a).
2. Hecke matrices U₃ = T₃ and T_ℓ, ℓ ∈ {7, 11, 13, 17, 19}, on the mfbasis; checkpoint each (`writebin`, one object per file — append gotcha).
3. Integrality: charpoly(U₃) ∈ ℤ[x] (basis-free); independent ordinary count by the 3-adic Newton polygon of the d-scaled charpoly with the i·v₃(d) correction. Expected 176 (from Stage 1; a different count is a finding, not a steer — the count I commit is the one my computation produces).
4. Stable lattice, fresh derivation: L ← L + Σ M_i L iterated to stability (denominators are 3-powers only; HNF via `mathnfmod`). Verification gates: all six operators integral on L; pairwise commutation exact; traces/charpolys preserved; det(transition) a power of 3. *(Note: block dims and packet polynomials are lattice-independent — composition data — so saturation quality affects speed, not the committed answer. My lattice need not equal the 07-26 lattice.)*
5. Joint mod-3 block decomposition of L/3L under {U₃, T₇, T₁₁, T₁₃, T₁₇, T₁₉}: deterministic primary refinement (split by U₃'s primary factors, refine by each T_ℓ). Per block: dim, U₃-packet (irreducible factors with multiplicity), tame packet tuple.
6. Ordinary sub-table: blocks with U₃-packet ≠ (x)-power, i.e. a₃ ≢ 0. Expected total dim 176 per branch.
7. Crossing classes: same tame 5-tuple, distinct U₃-packets, with the branch's weight-1 relation on the U₃-pair — αβ ≡ χ₋₄(3) = −1 (χ₁₂ branch), αβ ≡ χ₋₂₀(3) = +1 (χ₆₀ branch) — read via norms/products in the packet fields mod 3.
8. If per-call budget allows, T₂₃ both branches (≈ 392 s each, one call each) **before** the commit; the five-prime bar is the pre-registered minimum and stands if 23 is infeasible (RK3).

**Emission side (answer-free, computed fresh):**
9. Machine-verify the quintic: irreducibility, disc = 2¹⁶·5⁶ (or correct my hand value), Galois group A₅ (`polgalois`).
10. Factor x⁵+20x+16 mod ℓ for ℓ ∈ {7, 11, 13, 17, 19, 23}; cycle type → Frobenius order (A₅ orders: 1, 2, 3, 5).
11. Machine-enumerate the 2.A₅-trace × μ₄-dressing orbit mod 3; compare to the pre-registered shape-sets; **selection uses the pre-registered sets verbatim**.

**Selector:**
12. A census block passes iff at every tame prime its a_ℓ-packet's irreducible factors lie in the allowed set for that prime's Frobenius order (polynomial equality mod 3; zero-class means packet = power of x). Candidates additionally carry the crossing structure of step 7. Output: candidate list per branch.

**Commit gate:**
13. Write both branch commit docs (templates pre-staged below — the 07-26 missed-gate lesson) to the project: full ordinary block tables + candidate lists + emission-side data. **No key material opens before both commits are lodged.**

**Grading (after commit only):**
14. Open: OT1 key JSON (Drive), χ₁₂ GRADED record, χ₆₀ SUCCESS doc in full. Grade embedding-consistently: for each candidate block (a local 𝔽₃-algebra with residue field 𝔽_q), the key's reduced eigensystem tuple (mod a prime over 3 in its coefficient field) must land in the block's Frobenius-orbit of joint eigenvalue tuples (u; t₇, …, t₁₉[, t₂₃]) simultaneously — one embedding for all primes at once, per the brief's torsor/equivariance rules (one gauge bit; twist-orbit class). Machine check, not hand comparison.
15. Cross-diffs: my χ₁₂ table vs. the 07-26 committed table (machine diff, C1); my χ₆₀ table and candidates vs. the χ₆₀ SUCCESS doc's recorded results; my grade vs. their grade.

## 4. Pre-registered outcomes

- **R-SUCCESS:** both branch candidate lists, committed blind, contain the key eigensystem embedding-consistently at ≥ 5 tame primes with the (α,β) crossing relation, and the χ₁₂ census diff is clean. The program's first blind replication.
- **R-PARTIAL:** census replicates (C1 clean) but selector or grade diverges — itemize the layer; the divergence is the result.
- **R-DIVERGENT:** census structural mismatch (RK1): dims / block count / packets differ from the committed χ₁₂ table. Adjudicate by the independent gates (Newton polygon, commutation, traces); report which implementation the evidence favors; **do not** tune my code toward the known table beyond fixing failures of the named gates — every post-hoc change is logged with its trigger.
- **RK2 (spec gap):** a tame prime with Frobenius order 1, or shape-set enumeration mismatch: selection by pre-registered sets; gap reported.
- **RK3 (infeasibility):** chunk/checkpoint; five-prime minimum stands; any narrowing disclosed in the commit docs.
- **RK4 (foreclosure event):** if any forbidden material enters context before both commits, the clean-room ABORTS loudly; work continues unblinded with the event on the record.

## 5. Deviations from the 07-26 protocol, declared now

- Mod-3 working level (as executed 07-26), not the brief's m ≥ 6; exact ℤ[x] charpolys retained where computed, so depth-refinement stays open as queue item (f), unchanged.
- Block indices are sort-artifacts; matching to the committed table is by content (dims + packets + tuples), with an explicit relabeling map in the diff.
- The χ₆₀ SUCCESS doc's §§1, 3, 4 stay closed until grading even though §3 (sixth prime) is census-side in character — the subagent extract noted its location outside §2 and it stays sealed; my ℓ = 23 work, if any, is independent.

## 6. Commit-doc templates (pre-staged, per branch)

> **Carrier Rung 4 — CLEAN-ROOM COMMITTED CENSUS OUTPUT, {χ₁₂ | χ₆₀} branch (lodged before the key opens)** · 2026-07-27 · census construction line (dims, orbits, lattice, verification gates passed) · ordinary block table (per block: dim, U₃-packet, tame tuple) · crossing classes with the branch relation · emission-side table (cycle types, allowed sets) · **selector candidate list** · deviations · "the key opens next."

*Offered, not self-filed; countersign gates the record. This pre-registration is committed to the project before step 1 runs. Same-family caveat standing: replicator and original are the same model family under the same corpus conventions — this replication is blind to the key, not to the method.*
