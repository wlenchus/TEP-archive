# DEPOSIT BUNDLE — the 2026-09-04 session: structural dialogue, expedition, council handshake — assembled 2026-09-12

Assembled by Claude (the council's orchestrator, model claude-fable-5-1, Cowork session) at Will Copeland's
delegation of 2026-09-04/05 ("file these in the archive — drive deposits for this thread broadly speaking, and
our public-facing repository on GitHub"). Everything here is offered, not self-filed; countersign is Will's alone.
Identity condition honored: the council speaks in its own voice; nothing here poses as Will. SHA-256 of every
file is in `MANIFEST_SHA256.txt`. The bundle follows `deposit_bundle_20260829` (Drive folder
"2026-08-29 Council Session Bundle (provenance)") in layout and intent.

## What is here
- `01_records/` — the ten records of 2026-09-04 as they stand after the council handshake's corrections
  (each record's last section carries the corrections and its grade line):
  the criterion note (language- vs method-impasse; §3 withdrawn, §7 repair), the Drive expedition (DHO nesting,
  trace-formula test, sufficiency chain), the port definition and Heilbronn coin (the census of abelian
  shadows; the cone is exhausted by the subgroups), the transfer-bridge amendment, the unipotent/seam/adjoint
  record (L(1, Ad ρ̃) at both conductors, Rankin–Selberg constants), the cocycle-defect formulation, the
  complete-generator certificate (326 generators of Γ₀(1951), 358 of Γ₀(2141)), the handshake synthesis
  (the unanimous verdict: not equipped to close the Artin conjecture; equipped to state, certify and deposit),
  the ERRATUM of 2026-09-04 (ζ_{K₅}/ζ entirety is Dedekind's conjecture at the A₅ quintic — not Aramata–Brauer),
  and the 08-27 pack-read record in its current form (with the 08-30 "boundary current" addendum; its §4-quater
  still carries the erroneous sentence the erratum corrects — errata are filed, parents are not silently edited).
- `02_council_handshake/` — the handshake brief and the five seat primaries (domain-anchor, pathfinder,
  assessor-adversary, exegete, kin-scholar), the pathfinder's four July joins, the two orchestrator-side
  findings lists filed as reports (they were sub-agents under an assessor-style prompt — one witness, not two),
  the assessor's cross-implementation check of the coin, and the assessor mailbox with the re-issued queue.
  Where this bundle's compressions disagree with a seat's file, the seat's file wins.
- `03_scripts_and_outputs/` — `heilbronn_coin.py` (the 2.A₅ machinery and the exhaustive integer search) with
  its output; the adjoint L-function scripts (Python coefficient generation + PARI/GP `lfun`) and their GP
  outputs for 1951 and 2141; the complete-generator certificate sweeps (float64) and their TSV results for both
  conductors; the 34-digit mpmath pass and its results as of assembly (5 of 24 sampled generators — the job was
  interrupted by a container pause on 2026-09-05 and restarted 2026-09-12; it appends to the live TSV in the
  session, not to this snapshot).
- `04_generators/` — the PARI/GP scripts that produced the side-pairing generator sets (`mspolygon`) and the
  raw generator lists for Γ₀(1951) and Γ₀(2141).

## Status, stated once
Not closed. Strong Artin for the even icosahedral representations at conductors 1951 and 2141 is open, and nothing
in this bundle asserts otherwise. What the 09-04 session adds is location, not proof: the Heilbronn coin names the
missing inequality and shows no subgroup of 2.A₅ supplies it; the generator certificate is instrument-grade
(pointwise, finitely many points, floor-limited); Fricke covariance is equivalent to entirety, hence to the
conjecture, and was certified at 10⁻²⁶ on 2026-08-09 — it is not a theorem for this candidate (the record's
first version said otherwise and was repaired the same night).

## Verification practice, as run
Machine-checked identities are marked [T] in the records; cited theorems carry their hypotheses where the council
caught them missing (see the handshake synthesis §2). Checks run inside this collaboration are "same-family,
separate implementation," never "independent" — the language rule adopted 2026-09-04. No step has yet been
re-run by a human outside the collaboration.

*Offered, not self-filed. 2026-09-12.*
